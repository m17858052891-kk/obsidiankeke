---
tags:
  - 八股
  - 项目面试
  - 行为序列
  - 特征工程
  - 数据架构
  - 推荐系统
  - 价格策略
  - 交易策略
created: 2026-07-23
---

# 司乘行为序列建设项目面试问答

> 本文按“面试官提问 → 推荐回答 → 可能追问”组织。  
> 事实边界：现有材料没有给出计算成本下降比例、模型指标提升或线上收益，面试时不要虚构数字；请在文末指标清单中补入真实结果。

## 0. 一分钟项目介绍

**面试官：请介绍一下司乘行为序列建设项目。**

**推荐回答：**

这个项目主要解决两个问题：第一，原来价格和交易模型大量使用近 7 天、30 天的统计特征，用户行为被压成均值和次数后，行为顺序、价格变化、供需上下文以及“看价但未发单”这类负反馈会丢失；第二，冒泡、发单、应答、取消和完单分散在不同链路，价格侧和交易侧重复加工，口径不一致。

我围绕一次完整出行尝试，也就是 Attempt，设计了“事件层—Enrich 层—序列样本层”三层架构。事件层统一承接冒泡到支付、取消的漏斗事件；Enrich 层分别建设尝试级和订单级宽表，避免把发单前价格决策与发单后履约强行混成一张万能宽表；序列层围绕具体任务的样本左表和预测时点，按需回填乘客价格敏感度、司机收益效率和接驾偏好等历史序列。

价格侧最重要的变化，是把观测起点前置到冒泡阶段，把“看价未发单”纳入序列；工程上，为解决亿级样本回查 90～120 天历史导致的范围 Join 和笛卡尔积膨胀，我把历史先压成 `ID + 时间戳 + 序号 + 事件键` 的窄索引，先根据样本锚点找到历史上下界并截断到最近 128 条，再回表取宽特征。这样把大宽表的无界范围 Join，转成窄索引上的有界检索和小范围回表，减少 Shuffle、扫描列和中间结果规模，为后续 DIN、SASRec、Transformer 等时序模型提供统一底座。

### 30 秒精简版

> 我做的是司乘双边长行为序列基建。核心不是简单 `collect_list`，而是先统一一次出行 Attempt 的业务语义，再拆成事件、尝试/订单 Enrich、任务序列三层；价格侧把历史窗口前置到冒泡未发单阶段，工程侧用预排号、锚点边界定位、长度截断再回表，解决亿级样本长历史 Join 膨胀，为价格和交易模型提供防穿越、可复用的序列输入。

---

## 一、项目背景与业务价值

## 1. 为什么要建设行为序列？统计特征有什么问题？

**推荐回答：**

统计特征会把一段历史压缩成一个数，例如 30 天完单次数、平均价格、取消率。这类特征计算便宜，但有三类信息损失：

1. 顺序损失：`低价→高价→不发单` 与 `高价→低价→发单` 可能有相同均价，但决策含义不同。
2. 上下文损失：同样一次取消，在高供需紧张和低供需紧张下意义不同。
3. 细粒度反馈损失：只看订单会漏掉“多次冒泡看价但未发单”，而这恰恰是价格敏感的重要信号。

序列特征保留每次行为的时间、价格锚点、供需、品类、状态转移，让模型自己学习 recency、frequency、trend 和 pattern，而不只依赖人工聚合。

**可能追问：统计特征是不是就没用了？**

不是。统计特征稳定、低延迟、抗噪声，适合作为长期画像和模型兜底；序列负责保留细节和动态变化。更合理的方案是统计特征与序列表示联合，而不是完全替代。

## 2. 为什么这个项目同时做司机和乘客两侧？

**推荐回答：**

出行平台是双边市场，价格和交易结果同时受乘客决策与司机供给影响：

- 乘客侧关注价格敏感度、冒泡到发单、加反勾、取消和品类选择；
- 司机侧关注应答、接驾距离、履约时长、流水效率、区域和品类偏好；
- 场景侧还要结合城市、格子和实时供需。

只建乘客序列会忽略供给响应，只建司机序列又无法解释需求侧看价和流失。统一事件语义后，两侧可以按各自实体构造序列，并在订单或 Attempt 锚点处融合。

## 3. 交易策略和价格策略为什么不能直接共用一张序列表？

**推荐回答：**

两者预测时点和关注漏斗位置不同：

- 价格策略在冒泡/展示价格时就要决策，必须包含看价未发单样本，并严格禁止使用发单后的信息；
- 交易策略通常以已发单订单为锚点，更关注应答、改派、接驾、取消和完单链路。

底层事件和 Enrich 可以复用，但最终样本粒度、锚点时间、标签和可用字段不同。因此我统一底座，不强行统一最终序列样本。

## 4. 这个项目的核心产出是什么？

**推荐回答：**

核心产出不是单个模型，而是一套可复用的时序特征底座：

1. 统一的漏斗事件语义和 Attempt 归因；
2. 尝试级、订单级两个职责清晰的 Enrich 表；
3. 按任务和预测时点构造序列的标准流程；
4. 长历史的有界检索和回表优化；
5. 时间穿越、改派、取消、重复冒泡等数据质量口径；
6. 支持后续价格、交易模型从统计学习升级到深度时序建模。

## 5. 这听起来像 ETL，算法价值在哪里？

**推荐回答：**

它不是单纯搬表。算法价值体现在三个层面：

1. 样本语义：用 Attempt 统一一次决策过程，决定什么是正负反馈，直接影响监督信号是否正确；
2. 因果时点：围绕预测时刻控制可见信息，防止模型离线穿越；
3. 表示能力：把价格、供需和漏斗转移保留为序列，使 DIN、SASRec、Transformer 能学习传统统计特征无法表达的行为模式。

数据基建决定了模型能看到什么、标签代表什么以及线上能否复现，往往比单独替换一个网络层影响更大。

---

## 二、为什么使用 Attempt 粒度

## 6. Attempt 是什么？为什么不用 Order 作为统一根节点？

**推荐回答：**

Attempt 表示乘客从产生出行意图、进入冒泡看价开始的一次完整出行尝试，根标识是去重后的 `distinct_bubble_trace_id`。一个 Attempt 可能：

- 看价后不发单；
- 发出一笔订单；
- 因改派产生多个 order_id；
- 最终取消或完单。

Order 只在发单后存在。如果用 order_id 做根节点，所有看价未发单样本都会消失，价格模型只看到“成功进入订单链路的人”，产生样本选择偏差。因此上层业务语义用 Attempt 统一，发单后的履约实体仍保留 order_id。

## 7. `trace_id`、`order_id`、`origin_order_id` 分别是什么？

**推荐回答：**

- `distinct_bubble_trace_id`：一次去重后的出行尝试根，用于把相关冒泡与订单归到同一 Attempt；
- `order_id`：实际发生某个订单事件的订单号，改派后会变化；
- `origin_order_id`：改派链路最初订单号，用来把多次改派的订单串成同一订单链。

它们解决的层次不同：Attempt 负责用户决策过程，origin order 负责改派链路，current order 负责事件事实。不能只保留其中一个。

## 8. 为什么同一 Attempt 下可能有多个 Bubble？

**推荐回答：**

乘客可能重复刷新、切换品类或重新询价，底表中会产生多个 bubble trace，但业务标签可能把它们归并成同一次出行尝试。因此事件层可以保留明细，Attempt Enrich 层要按明确规则去重或聚合，例如选择决策前最后一次有效展示，同时保留冒泡次数、价格变化等信息。

如果无脑只保留最后一次，会损失价格试探过程；如果无脑全部当独立样本，又会造成样本重复和泄漏，所以要区分“事件保真”和“样本去重”。

## 9. 改派链路怎么处理？

**推荐回答：**

不把改派后的 order_id 覆盖成一个订单，也不把同类事件全部压成一条。每个事件保留实际 `order_id`，同时用 `origin_order_id` 指回链路根。例如：

```text
原始单 A：call → answer
改派单 B：call → answer
改派单 C：call → answer → arrive → finish
```

事件明细保留 A、B、C，链路分析按 origin A 聚合，最终履约信息来自 C。这样既能还原多次改派过程，也能计算改派次数、每段等待时长和最终履约结果。

## 10. `event_id` 如何保证唯一？

**推荐回答：**

Bubble 事件可以使用 `bubble_<bubble_trace_id>`；订单事件至少组合 `event_type + origin_order_id + order_id`。但严格工程上还应检查同一订单是否可能多次产生同类事件或底表重放，必要时再加入 `event_time`、source version 或稳定业务 event id，并通过唯一性监控验证，而不是仅凭字符串拼接假定绝对唯一。

---

## 三、三层架构设计

## 11. 为什么设计“事件—Enrich—序列”三层？

**推荐回答：**

三层分别解决事实统一、特征复用和任务适配：

```text
事件层：统一发生了什么
   ↓
Enrich 层：补齐下游高频复用的业务属性
   ↓
序列层：围绕具体样本和预测时点组织模型输入
```

如果直接从几十张源表为每个模型拼最终序列，口径会重复且难以治理；如果直接落一张万能序列大宽表，又会把不同预测时点、粒度和特征集合强行绑定。三层设计在统一与灵活之间做折中。

## 12. 事件层 `trip_funnel_events_di` 的粒度是什么？

**推荐回答：**

逻辑上是一行一个漏斗事件。订单事件粒度可理解为：

```text
event_type + origin_order_id + order_id
```

Bubble 事件按 bubble trace 保留。统一字段包括 passenger_id、event_type、event_time、Attempt 根、订单 trace、当前订单、改派根订单、城市和分区。

事件类型覆盖 bubble、call、answer、pickup_arrive、charge_start、finish、pay，以及应答前取消、应答后乘客/司机/客服取消和系统取消。

## 13. 为什么 Attempt Enrich 和 Order Enrich 不合并？

**推荐回答：**

因为一对多关系和字段可用时点不同：一个 Attempt 可以没有订单，也可以通过改派关联多个订单；Attempt 级字段是冒泡展示价格、供需和看价决策，Order 级字段是应答、履约、取消和完单属性。

强行合并会导致：

- 未发单 Attempt 丢失或产生大量 Null；
- 改派造成 Attempt 字段重复；
- 发单后字段容易穿越到价格预测时点；
- 表的粒度难以解释。

分开建设能保证主键和时间语义清晰，下游按任务 Join。

## 14. 为什么序列层不固化成一张最终大宽表？

**推荐回答：**

不同任务有不同：

- 样本左表；
- 预测锚点；
- 历史窗口；
- 最大长度；
- 可见字段；
- 标签与序列实体。

例如价格模型以 Bubble Attempt 为锚，完单模型以订单某时点为锚。如果固化唯一大宽表，要么包含未来字段造成泄漏，要么为了兼容所有任务极度稀疏和臃肿。因此序列层更适合作为按任务生成的 View/Job 和统一构造规范。

## 15. 事件层 V0 不要求实体化，会不会失去三层架构的意义？

**推荐回答：**

不会。三层首先是逻辑分层和数据契约，不一定首版全部物理落表。V0 可以先交付 Attempt/Order Enrich 与序列构造，但字段命名、事件类型、主键和时间语义按事件层标准设计。等复用任务增多或重复扫描成本上升，再把事件层实体化。

这是用最小交付验证价值，同时为后续演进保留一致接口。

---

## 四、预测时点与防止数据穿越

## 16. 序列构造最重要的时间条件是什么？

**推荐回答：**

对样本锚点时间 $t_i$，历史事件必须满足：

$$
t_i-W\le event\_time<t_i
$$

其中 $W$ 是 90 或 120 天窗口。严格线上一致性还要检查：

$$
available\_time<t_i
$$

因为某个事实虽早已发生，但如果数据在预测时刻尚未产出，离线使用同样属于 Feature Availability Leakage。

## 17. 为什么只写 `e.dt < s.dt` 不够？

**推荐回答：**

它能粗粒度防止使用未来分区，但有两个问题：

1. 会丢掉样本当天、锚点之前的有效历史；
2. 如果改成 `e.dt <= s.dt`，又必须用精确 `event_time < anchor_time` 防止同日未来事件泄漏。

更稳妥的做法是分区字段负责裁剪扫描范围，时间戳负责严格 As-of 语义。分区不是业务时间判断的替代品。

## 18. 为什么把乘客观测窗口前置到冒泡阶段？

**推荐回答：**

价格模型的决策发生在展示价格附近。如果历史只从已发单订单中取，模型看不到因为价格过高而流失的人，只能在“已经接受某个价格的人”上学习，存在明显选择偏差。

前置到 Bubble 后，可以观察：

- 看到了什么价格区间；
- 当时供需状态如何；
- 是否发单；
- 选择了哪个品类；
- 是否多勾、加勾或反勾；
- 多次看价之间价格和决策如何变化。

这能更直接刻画价格敏感度。

## 19. 看价未发单样本的价值是什么？

**推荐回答：**

它是需求侧的重要负反馈。只有发单样本时，模型容易把历史支付价当作可接受价格上限；加入未发单 Attempt 后，模型才能学习某些价格、供需和场景组合下用户选择退出。

但未发单不一定由价格导致，也可能是改变目的地、等待、比价或无真实出行需求，因此需要结合展示价格、重复冒泡、供需和后续行为，而不能把所有未发单都直接解释为价格拒绝。

## 20. 哪些字段最容易造成穿越？

**推荐回答：**

高风险字段包括：

- 最终是否完单、支付和实付金额；
- 应答后取消类型；
- 实际接驾/送驾时长；
- 最终完单品类；
- 发单后补贴或改派结果；
- 由未来分区回刷产生的状态位。

价格预测时只能使用冒泡时已经产生的展示、预估、供需和历史事件。交易模型也要按自己的预测节点切分，例如应答预测不能使用应答后的到达和取消。

## 21. 跨天事件怎么避免漏数？

**推荐回答：**

源表可能在 D 或 D-1 分区记录事件，事件本身的时间又落在目标自然日。可以扫描 D-1 到 D 两个源分区，再按 `TO_DATE(event_time)=biz_date` 写入目标静态分区，并用对应 `is_td_*` 状态确认该事件属于当天口径。

这种方式解决底表跨天更新和重复存储，但还应对迟到超过一天的事件设计回刷窗口和幂等重跑，而不能假设所有迟到数据都只跨一天。

---

## 五、长序列计算与性能优化

## 22. 最初的性能瓶颈是什么？

**推荐回答：**

朴素方案是样本表与 90～120 天历史宽表按 ID 和时间范围 Join：

```sql
s.id = h.id
AND h.event_time < s.anchor_time
AND h.event_time >= s.anchor_time - window
```

如果一个高频用户有 $S$ 个样本锚点和 $H$ 条历史，Join 中间结果最坏接近 $S\times H$。在亿级样本、宽表数百列的情况下，会产生：

- 巨大 Shuffle；
- 中间结果膨胀；
- 热点 ID 数据倾斜；
- 宽列反复复制；
- 聚合前才截断，前面计算已经浪费。

## 23. “ID + 时间戳预排号、区间截断后回表”具体怎么做？

**推荐回答：**

我把流程拆成四步：

### 第一步：历史窄索引

历史数据先只保留：

```text
entity_id, event_time, sequence_no, event_key, dt
```

按 `(entity_id, event_time, stable_event_id)` 排序，生成稳定的 `sequence_no`。宽特征仍留在 Enrich 表中。

### 第二步：定位样本锚点边界

对每个样本锚点找到：

- `right_no`：严格早于 anchor time 的最后一条历史；
- `window_left_no`：不早于窗口起点的第一条历史。

### 第三步：先做长度截断

若最大长度为 $K=128$：

$$
start\_no=\max(window\_left\_no,right\_no-K+1)
$$

候选范围变成：

$$
sequence\_no\in[start\_no,right\_no]
$$

### 第四步：小范围回表

只对最多 128 个 `event_key` 回 Join Attempt/Order Enrich，取模型真正需要的价格、供需、品类和履约字段。

核心是先在窄索引上确定“要哪几行”，再读取宽数据，而不是先复制整张宽表再聚合截断。

## 24. 预排号为什么能降低计算量？

**推荐回答：**

它本身不会神奇消除所有 Join，价值在于把时间范围检索转成整数边界，并让长度限制能够提前下推：

```text
原方案：范围 Join 全部历史 → 复制宽列 → collect → sort → 截 128
优化后：窄索引找边界 → 最多保留 128 个键 → 回表取宽列
```

中间结果从“每个样本窗口内的全部历史宽行”变成“每个样本最多 $K$ 条窄键/宽行”。当高频用户 120 天有上千行为时，收益最明显。

## 25. 如何保证 Sequence Number 稳定？

**推荐回答：**

只按时间戳排序不够，因为多个事件可能同毫秒发生。排序键至少要包含：

```text
entity_id,
event_time,
event_priority,
stable_event_id
```

还要定义相同时间下 bubble、call、answer 等事件的业务优先级。离线回刷时排序规则必须确定，避免相同数据多次重跑产生不同序列。

## 26. `COLLECT_LIST` 能保证时间顺序吗？

**推荐回答：**

不能依赖分布式执行中的输入顺序。安全做法是先收集包含排序键的 Struct，再显式排序，最后取值，例如：

```sql
TRANSFORM(
  SORT_ARRAY(
    COLLECT_LIST(NAMED_STRUCT(
      'seq_no', e.sequence_no,
      'event_id', e.event_id,
      'price', e.price
    ))
  ),
  x -> x.price
)
```

但如果先收集全部 120 天历史再排序，仍会产生大中间结果，所以应先通过边界将候选限制到 128 条，再做有序聚合。

## 27. 如何处理高频用户造成的数据倾斜？

**推荐回答：**

可以从数据和计算两侧处理：

1. 提前长度截断，防止极高频 ID 无限展开；
2. 对索引表按 ID 分桶并按时间排序，减少无序 Shuffle；
3. 对超级热点 ID 单独识别和分片；
4. 在 Spark 中使用 AQE/Skew Join，但它只是工程兜底；
5. 监控每个 ID 的历史长度 P50/P95/P99/Max；
6. 对异常机器流量、重复事件先去重，而不是把脏数据当作真实高频。

## 28. 为什么窗口是 120 天、长度是 128？

**推荐回答：**

两者控制不同维度：120 天限制时间跨度，128 限制事件数量。长时间窗口让低频乘客也有足够历史，最大长度控制模型计算和样本存储。

这不是理论最优值，应通过行为覆盖率和模型消融确定：比较 30/60/90/120 天以及 32/64/128/256 长度，观察非空率、截断率、模型收益、训练成本和线上延迟。高频用户可保留最近 128 条，低频用户则覆盖更长自然时间。

## 29. 怎么证明优化真的有效？

**推荐回答：**

不能只说“跑快了”，应比较优化前后：

- 扫描字节数；
- Shuffle Read/Write；
- 中间行数峰值；
- Spill、Peak Memory；
- Stage P95 时长和总 CPU-hours；
- 热点 Task 最大耗时；
- 每个样本最终序列一致率；
- 特征产出耗时与成功率。

当前材料没有具体数字，面试前应补入真实口径，例如“Shuffle 降低 X%、任务耗时由 A 降至 B、序列一致率 100%”。没有数据就回答验证方法，不要编造。

---

## 六、特征设计

## 30. 乘客侧序列包含哪些特征？

**推荐回答：**

可以按五类组织：

1. 时间：冒泡时间、周几、距前一冒泡/完单间隔；
2. 行程：起终点城市与格子、跨城、预估距离与时长；
3. 价格：最低/最高实付展示价、价格锚点、相对特惠价、发单价；
4. 选择：是否发单、多勾、加勾、反勾、品类选择；
5. 上下文：格子与城市 CR/ECR、在线司机数等供需特征。

关键是每个时间步同时保留“用户看到了什么、当时环境是什么、最后做了什么”。

## 31. 司机侧序列应该如何设计？

**推荐回答：**

司机侧可围绕供给效率和偏好组织：

- 收益：每单收入、单位在线时长收益、单位里程收益；
- 效率：接驾距离/时长、空驶、履约时长、连续接单间隔；
- 决策：曝光/派单、应答、拒绝、取消；
- 偏好：城市、区域、品类、距离段、时段；
- 上下文：当时供需、热区、天气和交通。

还要明确司机序列的 anchor 是派单前、应答前还是调度时，不同锚点可见字段不同。

## 32. 为什么把供需特征放入序列，而不只放当前供需？

**推荐回答：**

同一个行为在不同供需下含义不同。例如乘客在供给充足时未发单，可能更像价格拒绝；在极端缺车时未发单，也可能是 ETA 过长。历史供需序列还能帮助模型区分稳定偏好与环境驱动行为。

但实时供需分布漂移快，必须保证离线和线上口径、时间粒度、延迟一致，并做好归一化和缺失处理。

## 33. 序列中连续特征和类别特征如何编码？

**推荐回答：**

- 类别特征：独立 Embedding 或共享词表 + Field Embedding；
- 连续特征：分桶 Embedding、标准化后线性投影，或同时保留连续值和分桶值；
- 时间间隔：Log Bucket、Time2Vec 或相对时间 Bias；
- 事件类型：Event-type Embedding；
- 缺失值：Missing Indicator + 默认值，不把“未知”与真实 0 混为一谈。

每个时间步可以把多个 Field 拼接后投影到统一维度，也可以把字段进一步 token 化，取决于模型和计算预算。

## 34. Attempt 序列与 Event 序列选哪个？

**推荐回答：**

Event 序列信息最细，但更长、重复和噪声更多；Attempt 序列把同一次出行决策聚合成一个时间步，更适合价格敏感和跨 Attempt 偏好建模。

可以分层使用：Attempt 序列建模长期出行决策，Attempt 内事件序列建模一次漏斗状态转移。V0 优先 Attempt/Order Enrich 能降低复杂度，后续再做层次化序列。

---

## 七、模型如何使用这些序列

## 35. 这些序列可以接哪些模型？

**推荐回答：**

- Mean/Sum Pooling：最低成本 baseline；
- GRU/LSTM：按顺序递推，适合中短序列；
- DIN：当前价格方案、品类或订单作为 Query，挑选相关历史；
- SASRec/Transformer：用位置和 Self-Attention 学习行为演化；
- HSTU 等推荐序列模型：面向长且变长行为流；
- 多塔/多任务模型：分别编码司机、乘客、场景，再预测发单、应答、取消、完单等任务。

基建层不绑定唯一模型，先保证语义、时点、Mask 和 Schema 稳定。

## 36. 价格模型如何使用乘客序列？

**推荐回答：**

当前候选价格方案可以作为 Query，历史每次 Attempt 包含价格锚点、供需和决策。通过 DIN/Cross-Attention 得到候选价格条件下的历史兴趣：

$$
u(q)=\sum_j\alpha(h_j,q)h_j
$$

这样模型能区分用户对低价、深折扣、特定供需场景的敏感历史，而不是把所有行为平均。也可以先用 SASRec 编码兴趣演化，再让当前价格 Query 读取序列状态。

## 37. 司机和乘客序列怎么融合？

**推荐回答：**

可以分别编码成：

$$
h_p=Encoder_p(S_p),\qquad h_d=Encoder_d(S_d)
$$

再与订单和供需上下文交互：

$$
z=f(h_p,h_d,x_{order},x_{supply})
$$

如果是候选司机排序，可用订单/乘客表示作为 Query 对司机历史做 Target-aware 聚合。为控制延迟，乘客和司机的候选无关历史表示可以缓存，只在最后做轻量 Cross。

## 38. 如何处理 Padding、截断和 Mask？

**推荐回答：**

- 序列按时间从旧到新或新到旧统一约定；
- 超长序列保留最近 K 条或按业务重要性采样；
- 短序列 Padding，并生成有效长度 Mask；
- Causal 模型还需 Future Mask；
- 连续字段的 Null Mask 与序列 Padding Mask 分开；
- Label 时间之后的事件在数据层已过滤，不能只依赖模型 Mask。

## 39. 线上推理如何保持一致？

**推荐回答：**

需要统一：

1. 事件定义和主键归因；
2. 特征计算 SQL/代码；
3. 窗口、排序、截断方向；
4. 缺失值、词表和归一化参数；
5. 数据可用时间；
6. 线上增量更新和离线回刷语义。

可采用统一 Feature Definition、样本回放和离在线 Diff 测试。否则离线模型即使有收益，线上也可能因序列少一条、顺序相反或状态延迟而失效。

---

## 八、数据质量与稳定性

## 40. 如何保证事件表质量？

**推荐回答：**

至少建立以下监控：

- 主键唯一率与重复率；
- event_time 非空率和时间合法性；
- 各 event_type 日趋势与漏斗转化；
- `call < answer < arrive < charge < finish < pay` 的时序合法率；
- Attempt 关联率、order 到 bubble trace 补齐率；
- origin_order_id 链路闭合率；
- 取消类型分布；
- 跨天迟到率和回刷影响；
- 城市、产品线和上下游源表对账。

## 41. 为什么订单事件要求对应 `is_td_* = 1`？

**推荐回答：**

底表可能因跨天更新、状态同步或重复存储，在多分区中出现类似生命周期时间。例如取消订单可能同步 finish time，但不代表真实完单。只有对应 `is_td_finish=1` 才能认定 finish 事件属于当日事实口径。

时间字段存在不等于事件真实发生，必须结合状态位。

## 42. 数据迟到和回刷怎么处理？

**推荐回答：**

每日任务应幂等，并为迟到数据设置滚动回刷窗口。需要区分：

- event_time：业务发生时间；
- ingest/update time：数据到达时间；
- dt：存储分区。

回刷后要更新受影响的 Enrich 和序列样本，并记录版本。如果训练集使用最终回刷结果，还要确认线上预测时是否可获得相同信息，避免 Availability Leakage。

## 43. 如何验证优化前后的序列完全一致？

**推荐回答：**

对一段固定样本同时运行朴素 Range Join 和优化方案，逐样本比较：

- 历史 event_key 集合；
- 顺序；
- 长度；
- 首尾时间；
- 每个字段值和 Null；
- Hash；
- 在重复时间、同日锚点、改派、高频用户、窗口边界上的 Case。

先证明语义等价，再比较性能；不能用运行更快换取悄悄丢历史。

---

## 九、难点、取舍和复盘

## 44. 这个项目最难的点是什么？

**推荐回答：**

我认为最难的不是写 SQL，而是同时保证三件事：

1. 业务语义正确：一次 Attempt 与多 Bubble、多改派订单如何归因；
2. 时间语义正确：不同任务在各自预测时点能看到哪些字段；
3. 计算可落地：亿级样本回查长历史时，不能让范围 Join 和宽表复制失控。

三者相互制约：粒度过细计算爆炸，过度聚合又丢信息；统一过度会造成穿越，拆得过散又无法复用。

## 45. 你做了哪些关键取舍？

**推荐回答：**

- 用 Attempt 统一业务根，但保留订单和事件明细，不做过度压缩；
- Attempt Enrich 与 Order Enrich 分开，牺牲“一张表查完”的便利，换取粒度和时点清晰；
- 序列层按任务生成，不固化万能宽表；
- V0 优先小猪端和关键加反勾字段，控制首版范围；
- 用 120 天窗口保证低频覆盖，用 128 长度控制计算；
- 先窄索引截断再回宽表，增加一层索引维护，换取大规模构造稳定性。

## 46. 这个方案还有哪些不足？

**推荐回答：**

1. V0 数据只覆盖单端，跨端行为仍然割裂；
2. 加反勾只保留是否，未保留数量和更细操作；
3. 补勾页发单尚未纳入；
4. 事件层若未物理化，不同任务仍可能重复扫描源表；
5. 128 最近行为可能偏向高频短期模式，丢掉长期周期性；
6. 未发单原因不可完全归因于价格；
7. 多次改派和迟到数据仍依赖底表 origin 字段质量；
8. 当前材料尚未给出端到端模型和线上收益闭环。

## 47. 下一步怎么演进？

**推荐回答：**

我会分三阶段：

1. 数据完善：实体化统一事件层，引入跨端行为、补勾页和更细加反勾数量；
2. 表示升级：做 Attempt 内漏斗编码 + 跨 Attempt 长序列的层次化模型，引入时间间隔和供需 Bias；
3. 系统闭环：建设近实时序列更新、离在线一致性监控，并通过模型消融和 A/B 实验量化每类序列的增量价值。

## 48. 如果模型没有提升，这个基建还有价值吗？

**推荐回答：**

要分层判断：

- 如果数据质量、覆盖和成本达标，但复杂模型无提升，统一口径和可复用 Enrich 仍能减少重复开发；
- 模型无提升可能说明任务不依赖顺序、序列噪声高、模型使用方式不对或统计特征已足够；
- 应做 `统计特征 → 简单 Pooling → DIN → SASRec` 的逐级消融，定位是数据无增量还是模型没利用好；
- 若长期多个任务都无收益且维护成本高，应缩减序列范围，而不是为了“用了 Transformer”继续投入。

---

## 十、面试官可能连续追问的压力测试

## 49. 为什么不直接对每个样本取最近 128 条？

**推荐回答：**

“取最近 128 条”是目标，不代表朴素 SQL 能低成本实现。若先 Range Join 120 天全部历史再 `row_number <= 128`，中间结果仍已膨胀。优化重点是把 Top-K 和时间边界尽量前推到窄索引检索阶段，再回表读取宽特征。

## 50. 使用 Row Number 后有新事件补入，历史序号不会变化吗？

**推荐回答：**

会，所以要明确索引版本和分区更新策略。若迟到事件插入历史中，之后序号可能重排；离线全量回刷可重新生成，增量系统则可使用分段序号、时间 + 稳定 event key，或让序号只作为查询辅助而不作为永久业务主键。真正稳定的事实键仍是 event key。

## 51. 为什么不把历史直接存成用户级 Array？

**推荐回答：**

用户级 Array 能减少 Join，但全量 Array 更新代价大，超长用户容易形成巨型行，按任意历史锚点做 As-of 截断也不方便。它更适合服务当前时点的最新序列；训练中每个用户有大量历史锚点，窄索引加边界定位更灵活。也可以按天/周分段存 Array，在性能和更新成本之间折中。

## 52. 价格锚点怎么定义？

**推荐回答：**

价格锚点必须是冒泡决策时真实可见、口径稳定的参照价格，例如某个主推品类或基准产品的实付展示价。还可以构造最低价/锚点、候选价/锚点等相对特征。

需要避免使用最终发单或完单后才能确定的价格作为冒泡时锚点，并按城市、品类和版本监控锚点覆盖率。

## 53. 取消为什么要区分应答前后和角色？

**推荐回答：**

不同取消代表不同机制：应答前取消可能与等待、价格或需求消失有关；应答后乘客取消可能与 ETA 和体验有关；司机取消反映司机偏好或履约困难；系统/客服取消又是另一类流程。混成一个 cancel 会让模型无法学习责任和阶段差异，等待时长也会算错。

## 54. 你怎么确认 Attempt 归并没有错？

**推荐回答：**

通过规则和抽样双重验证：

- 同一 Attempt 的事件时间应连续且城市/起终点基本一致；
- 发单前最近有效 Bubble 应能关联到订单；
- 不同 distinct bubble 不应因 passenger_id 相同被误合并；
- 多次改派应共享 origin order，但保留不同 order_id；
- 抽取典型链路可视化，人工核对 Bubble→Call→Answer→Finish/Cancel。

还应监控每 Attempt 的 Bubble 数、Order 数和异常长链分布。

## 55. 如果一个用户同时有两次出行意图怎么办？

**推荐回答：**

不能只按 passenger_id 和时间邻近强行合并，必须优先使用业务 trace 和去重标签。若 trace 缺失，需要结合时间、起终点、会话或订单关联做兜底，并标记低置信度。对并发 Attempt，事件应分别归属，不然会把两个目的地的价格与行为串成一条错误序列。

---

## 十一、需要补充的真实项目指标

面试前请用真实数据补齐以下内容：

| 类型 | 建议指标 | 你的真实结果 |
|---|---|---|
| 数据规模 | 日增事件数、用户数、样本数、120 天历史量 | 待补充 |
| 覆盖率 | Attempt/Order 关联率、非空序列率、司机/乘客覆盖率 | 待补充 |
| 序列分布 | 长度 P50/P90/P99、128 截断率 | 待补充 |
| 质量 | 主键重复率、事件时序合法率、迟到率 | 待补充 |
| 性能 | 任务时长、Shuffle、扫描量、CPU-hours 降幅 | 待补充 |
| 模型 | AUC/GAUC/LogLoss 或业务指标增量 | 待补充 |
| 线上 | 收益、转化、完单、成本、延迟 | 待补充 |

### 没有完整收益数字时怎么回答？

> 这个阶段主要完成了数据底座和计算链路，模型增量仍在验证，因此我不会把未验证收益说成已上线结果。已经确认的是覆盖率、序列一致性和计算稳定性；模型侧会通过统计特征、Pooling、DIN/SASRec 的控制变量实验，以及线上 A/B 进一步量化。

---

## 十二、最后背诵版

### 项目目标

> 解决统计特征信息压缩和冒泡—订单—履约链路割裂，建设司乘双边长序列底座。

### 核心粒度

> Attempt 统一一次出行意图，Order 保留发单后履约实体，Event 保留最细状态变化。

### 三层架构

> 事件层统一事实，Attempt/Order Enrich 复用业务属性，序列层围绕任务锚点按需构造。

### 价格侧关键改动

> 把观测窗口前置到 Bubble，纳入看价未发单，使模型能学习价格和供需上下文下的退出决策。

### 性能优化

> 历史先建 ID、时间戳、序号、事件键的窄索引；样本锚点先定位上下界并截到最近 128 条，再回宽表取特征，避免宽表无界范围 Join。

### 防穿越

> 分区只负责裁剪，严格使用 `event_time < anchor_time`，必要时再约束 `available_time < anchor_time`。

### 项目价值

> 统一业务语义和特征口径，降低长历史构造成本，并为 DIN、SASRec、Transformer 等模型提供可复用、可防穿越的时序输入。

