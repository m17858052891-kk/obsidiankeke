---
tags: [司乘序列, 特征工程, 时序建模, 面试]
---

# 司乘行为序列建设：核心 10 问

## 0. 一分钟项目介绍

项目解决统计特征损失序列信息、冒泡与订单链路割裂的问题。以一次出行尝试 Attempt 为统一根节点，设计“事件—Enrich 宽表—任务级序列样本”三层结构，沉淀乘客价格敏感和司机履约/收益偏好序列。价格侧将观测时点前置到冒泡未发单；针对亿级历史回查，用“ID+时间戳预排号、样本锚点截断候选区间、再回表取数”避免朴素 range join 的笛卡尔膨胀。

## 1. 为什么要建设行为序列？

统计特征只保留均值、次数等压缩结果，会丢失行为顺序、间隔、价格锚点和供需状态变化。交易侧关注发单后履约，价格侧还要理解“看价未发单”，因此需要统一完整出行漏斗的历史底座。

## 2. 为什么统一粒度是 Attempt，而不是 Order？

Order 只在发单后存在，无法容纳冒泡未发单样本。Attempt 以 `trace_id` 表示一次出行意图，可连接冒泡、发单、应答、取消、完单；`order_id` 在未发单时允许为空，因此更适合统一价格和交易链路。

## 3. 三层架构分别做什么？

- Event：漏斗动作明细，逻辑粒度为 `trace_id + order_id + event_type + event_id`；
- Attempt/Order Enrich：可复用的尝试级、订单级中间宽表；
- Sequence Samples：围绕具体任务左表和预测时点回填序列，不固化为唯一万能训练大宽表。

## 4. 为什么 Attempt Enrich 与 Order Enrich 不合并？

两者覆盖范围与时间语义不同：Attempt 保留全部冒泡和发单前信息，Order 承接发单后的履约链路。合成万能宽表会造成大量空字段、口径冲突和不必要扫描；训练时应按任务预测时点 join 所需 Enrich。

## 5. 为什么把价格侧观察窗口前置到 Bubble 未发单？

发单样本已经经过用户的价格筛选，只看订单会漏掉“看价后放弃”的关键反事实。Bubble 阶段能记录不同价格锚点、供需和品类展示下的决策变化，但未发单不应被粗暴解释为价格拒绝，也可能是需求消失或外部因素。

## 6. 序列防穿越最重要的条件是什么？

历史事件必须在样本预测时点前**已经发生且已经可用**。仅写 `e.dt < s.dt` 不够，因为同日存在时分秒顺序、跨天迟到和回刷；应使用 event time、available time、稳定 tie-breaker，并按预测时点截断。

## 7. 朴素历史 Range Join 为什么会爆炸？

若每个训练样本直接 join 同一用户 120 天内全部历史，样本数乘平均历史长度会产生巨量中间行；高频用户和热点 ID 更会产生数据倾斜。问题不在最终只取 128 条，而在 join 时已经扫描和复制了全部候选历史。

## 8. “预排号 + 区间截断 + 回表”如何优化？

先在历史窄索引表按 `(entity_id, event_time, stable_id)` 排出 sequence number；样本只定位锚点前的序号区间，如最近 128 条/120 天；随后仅对小区间回表取完整字段。这样将“大范围 join 后再过滤”改为“先缩小候选，再取数”。

## 9. 为什么窗口 120 天、长度 128？如何保证顺序？

120 天覆盖价格和出行习惯的中期记忆，128 控制训练成本；两者是业务覆盖与模型吞吐的折中，应经消融验证。`COLLECT_LIST` 本身不能保证顺序，必须在聚合前按 event time 与稳定二级键排序，或聚合结构体后排序再拆字段。

## 10. 这不只是 ETL，算法价值是什么？

它将原本割裂的漏斗行为变成可被 DIN、SASRec、HSTU 等模型读取的、时间正确的序列输入，并让价格模型获得未发单的决策证据。真正价值是定义统一样本锚点、避免未来泄漏、控制长序列计算成本，并为后续深度时序建模提供可复用特征底座。

