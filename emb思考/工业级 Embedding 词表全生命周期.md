在深度学习推荐系统（如 CTR 预估）中，将人类可读的离散属性（如“城市”、“商品ID”）转化为神经网络可计算的稠密向量（Dense Vector），是一个精密而严谨的工程。

我们将这个过程分为两大阶段：**造表（构建词表确定行数）** 与 **填表（初始化确定数值）**。最后，我们将通过一个完整的端到端案例来串联这一切。

# 阶段一：造表 —— 词表构建流水线 (Vocabulary Construction)

# 目录

- [# Step 1: 收集原始日志](#step-1-收集原始日志)
- [# Step 2: 频次统计与截断](#step-2-频次统计与截断)
- [# Step 3: 构建映射字典 (Mapping Dict)](#step-3-构建映射字典-mapping-dict)
- [# Step 4: 矩阵初始化 (Initialization)](#step-4-矩阵初始化-initialization)
- [# Step 5: 线上真实推理 (Inference)](#step-5-线上真实推理-inference)

这一步的目的是确定矩阵 $W$ 的形状：$V \times D$。 维度 $D$ 是我们人为设定的（比如 16、64）。而词表大小 $V$，需要经过以下严密的流水线来确定：

1. **频次统计与截断 (Frequency Filtering)：** 扫描全量训练数据，统计每个离散值出现的次数。设定阈值（如频次 < 5 的直接丢弃），防止极其罕见的长尾噪声撑爆显存，同时也因为它们样本太少，根本学不出有效的向量。
2. **保留特权席位 (Special Tokens)：** 在分配真实的 ID 之前，必须先在词表的最前面预留两个特殊的“VIP 座位”：
    - **`[PAD]` (Padding, ID=0)：** 用于对齐不定长序列的补位符。
    - **`[UNK]` (Unknown / OOV, ID=1)：** 兜底符。用来收留那些被低频过滤掉的特征，或者未来线上突然出现的新特征。
3. **连续 ID 映射 (String to Integer Mapping)：** 将截断后幸存下来的高频特征，按频次或字典序，从 ID=2 开始依次分配连续的整数索引。最终得到一个静态的映射字典（String -> ID）和最终的词表总行数 $V$。

# 阶段二：填表 —— 权重矩阵初始化 (Matrix Initialization)

现在我们得到了一个形状为 $V \times D$ 的空矩阵 $W$。接下来要往里面填入初始的浮点数。工业界有严格的规矩：

1. **绝对禁忌区（Row 0 = `[PAD]`）：** 矩阵的第一行，必须被强制覆写为 **全零向量 (All-Zero Vector)**。

    $$W_0 = [0.0, 0.0, \dots, 0.0]$$

    并在深度学习框架中设置 `padding_idx=0`，告诉优化器在反向传播时**绝对不更新**这一行，让它永远保持“虚无”。

2. **常规军（Row 1 到 V-1）：** 使用极小方差的正态分布（如 $\mathcal{N}(0, 0.01^2)$）或者截断正态分布进行随机填充。
    - 为什么不用标准正态分布 $\mathcal{N}(0, 1)$？因为在后续的点积或全连接层中，方差为 1 的向量相乘极易产生极大的输出值，导致 Sigmoid 或 ReLU 直接饱和，引发梯度消失（模型第一步就“死”了）。

# 🚀 终极实战演练：以“用户所在城市”为例

假设我们正在做一款本地生活 APP 的推荐系统，我们要处理“用户城市 (City)”这个特征。设定的 Embedding 维度为 $D=4$。

# Step 1: 收集原始日志

我们在近 7 天的日志中收集到了以下 8 条数据： `["北京", "上海", "广州", "北京", "上海", "北京", "铁岭", "鹤岗"]`

# Step 2: 频次统计与截断

统计频次：`{"北京": 3, "上海": 2, "广州": 1, "铁岭": 1, "鹤岗": 1}` 设定低频阈值为 **2**。 过滤后存活的城市：`["北京", "上海"]`。 被淘汰的城市：`["广州", "铁岭", "鹤岗"]`。

# Step 3: 构建映射字典 (Mapping Dict)

预留 0 和 1，给存活的城市分配 ID：

```text
{
  "[PAD]": 0,
  "[UNK]": 1,
  "北京": 2,
  "上海": 3
}

```

此时，确定词表大小 $V = 4$。我们要构建一个 $4 \times 4$ 的矩阵 $W_{city}$。

# Step 4: 矩阵初始化 (Initialization)

在模型 `__init__` 阶段，通过 PyTorch/TensorFlow 生成初始矩阵：

$$W_{city} = \begin{bmatrix} 0.000 & 0.000 & 0.000 & 0.000 \\ -0.012 & 0.005 & 0.008 & -0.003 \\ 0.004 & -0.009 & -0.001 & 0.007 \\ -0.002 & 0.011 & 0.006 & -0.005 \end{bmatrix} \begin{array}{l} \leftarrow \text{ID: 0 ([PAD], 强制全0)} \\ \leftarrow \text{ID: 1 ([UNK], 极小正态分布)} \\ \leftarrow \text{ID: 2 (北京, 极小正态分布)} \\ \leftarrow \text{ID: 3 (上海, 极小正态分布)} \end{array}$$

# Step 5: 线上真实推理 (Inference)

模型上线了，此时来了三个请求。

- **用户 A 来自“北京”：** 查字典：`北京 -> ID: 2` 查表：提取矩阵第 3 行 $\rightarrow [0.004, -0.009, -0.001, 0.007]$
- **用户 B 来自“广州”（注意，它在训练期被截断了）：** 查字典：找不到“广州”，命中兜底逻辑 $\rightarrow$`ID: 1 ([UNK])` 查表：提取矩阵第 2 行 $\rightarrow [-0.012, 0.005, 0.008, -0.003]$
- **用户 C 的序列不足，需要补齐 (Padding)：** 系统强行补上补位符 $\rightarrow$ `ID: 0 ([PAD])` 查表：提取矩阵第 1 行 $\rightarrow [0.000, 0.000, 0.000, 0.000]$ （完美避免对网络产生任何数值干扰）。

# 总结

从收集字符串到变成神经网络里的浮点数矩阵，这就是一个离散特征的完整一生。在真实的淘宝、字节体系中，这个流程的逻辑完全一样，只不过“城市”变成了上亿规模的“商品 ID”，而截断阈值和分布式哈希映射（Hashing）的手段会更加极端和硬核。
