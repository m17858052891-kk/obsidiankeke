1. ipw给当前treatment组和control组的信息（2y-1）

call的情况下，T分为正，C分为负

非call的情况下，T分为负，C分为正

这是我给到每个组的简要信息（数值没有意义，主要是正负信息）

2、对score和伪标签进行相关性计算，对原本打分相同的样本，如果一个为正一个为负，原本分数为负的样本相关性更低，1-corr更高，就对梯度进行惩罚。目的在于对打分类似的不同样本的正负向进行区分。这里通过调整loss-梯度-模型参数，让score分数正向的向上，负向的向下，这里给到的信息就是广义的排序信息，并不代表真的有完整的对ABCD就显式的1234排序。

这里不管是用相关性系数，还是其他什么函数都是可以的，只要是能够把伪标签信息正确映射到打分上，对其进行正负向的驱动就行。
