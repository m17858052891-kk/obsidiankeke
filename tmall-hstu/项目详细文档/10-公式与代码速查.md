# 10 公式与代码速查

## 核心公式

$$
w_{ij}\mathrel{+}=b(a_i)b(a_j)e^{-|t_i-t_j|/\tau_t}
\frac{1}{\log(1+L_u)}\gamma^{d(i,j)-1}
$$

$$
s(i,j)=\hat E_i^T\hat E_j,\qquad \hat E_i=E_i/\|E_i\|_2
$$

$$
\mathcal{L}_{pair}=-w_{ij}\log\sigma(s(i,j))
-\sum_{n\in N(i)}\log\sigma(-s(i,n))
$$

$$
r_0=z,\quad k_l=\arg\min_k\|r_l-C_l[k]\|^2,\quad r_{l+1}=r_l-C_l[k_l]
$$

$$
L_{SID}=\sum_l\lambda_lCE_l
$$

$$
[Q,K,V,U]=XW,\quad A=SiLU(QK^T+B),\quad H=AV
$$

$$
Y=X+((H\odot U)W_o)
$$

## 张量和伪代码

```text
history_sid_codes : [B, T, L_sid]
history_embedding  : [B, T, D]
HSTU hidden        : [B, T, D]
target_sid_codes   : [B, L_sid]
code logits level  : [B, K_l]
beam prefix        : [beam_size, current_level]
```

```python
pairs = weighted_cooccurrence(user_sequences)
item_vec = train_item2vec(pairs, negative_sampling=True)
sid_codes = rqvae.fit_transform(item_vec)
sid_table = build_sid_table(item_ids, sid_codes, collision=True)
prefix_next = build_prefix_index(sid_table)
history_repr = hstu(history_sid, behavior, time_gap)
loss = teacher_forced_sid_loss(history_repr, target_sid)
pred_sid = constrained_beam_search(history_repr, prefix_next)
items = map_sid_to_items(pred_sid)
```

## 评估速记

```text
效果：HR@K, NDCG@K
覆盖：Coverage@K, Tail Recall, Diversity
结构：Invalid SID Rate, Collision Rate, Code Usage
成本：训练时间、显存、解码延迟、beam 分支数
```
