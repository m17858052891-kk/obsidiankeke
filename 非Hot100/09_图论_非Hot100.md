# 非 Hot 100 — 图论

> 这些题不属于正式 Hot 100，但仍有较高面试价值。

## 210. 课程表 II

**题目描述：** 返回完成所有课程的学习顺序，若无法完成则返回空数组。

**示例：** 输入：`numCourses = 2, prerequisites = [[1,0]]`；输出：`[0,1]`。

**思路：** 同 #207，在拓扑排序时记录处理顺序。

**展开理解：** 课程表 II 不只判断能否完成，还要输出一个拓扑序。把所有入度为 0 的课程入队，每弹出一门就加入答案并削减后继入度；若最终答案长度小于课程数，说明有环，此时应返回空数组。

```python
from collections import deque

def findOrder(numCourses, prerequisites):
    graph = [[] for _ in range(numCourses)]
    in_degree = [0] * numCourses
    for a, b in prerequisites:
        graph[b].append(a)
        in_degree[a] += 1
    queue = deque(i for i in range(numCourses) if in_degree[i] == 0)
    order = []
    while queue:
        node = queue.popleft()
        order.append(node)
        for nei in graph[node]:
            in_degree[nei] -= 1
            if in_degree[nei] == 0:
                queue.append(nei)
    return order if len(order) == numCourses else []
```

**复杂度：**
- 时间：O(V + E)
- 空间：O(V + E)
