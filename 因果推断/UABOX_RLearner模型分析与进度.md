# UABOX R-learner 模型分析与进度

> 更新时间：2026-09-14（CST）  
> 项目目录：`/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model`

## 文档逻辑导航

本文按照“目标 → 数据 → 基线模型 → 基线结果 → 误差诊断 → 架构实验 → 当前结论”的顺序展开：

1. **业务目标**：第 1 章明确模型要模拟的对象，以及绝对响应和 CATE 两类输出。
2. **数据与因果口径**：第 2 章说明样本、切分和 Treatment 覆盖；第 3–4 章讨论未开城供需分布、两条数据链路和 Label 口径风险。
3. **模型方案**：第 5 章先说明独立 Treatment 头基线，再汇总纯低秩头和低秩残差头两个实验变体。
4. **基线结果**：第 6 章记录删除 `city_id` 前后的整体和分档测试结果。
5. **剩余误差是否可学习**：第 7 章记录 DR 残差方法、失败修复过程和最终结果；第 8 章给出判断标准。
6. **历史判断**：第 9 章完整保留 DR 修复前的阶段性结论，作为决策过程记录，不代表当前最终状态。
7. **专项诊断与架构实验**：第 10 章依次记录 DR 修复、85/91 折专项诊断、Treatment 头实验和后续候选方案。
8. **当前结论**：第 11 章汇总截至本次整理时最应采用的判断和实验状态。
9. **如何验收冒泡级仿真**：第 12 章解释为什么不能用普通准确率衡量 CATE，并根据效应信号、测试集噪声和业务用途给出分级门槛。

若只阅读当前有效结论，建议依次查看第 1–2 章、第 4 章、第 6 章、第 7.4 节、第 10.2 节、第 10.3 节和第 11 章；第 7.3、9、10.1 节属于保留的历史过程。

## 1. 项目目标

在幸运盒子真正未开城的环境中，训练一个冒泡粒度的多 Treatment 因果响应模型，模拟同一个冒泡在不同特价盒子折扣下的完单概率：

$$
P\bigl(Y=1\mid do(T=a),X\bigr),\quad a\in\{82,85,88,91,94,97,100\}
$$

其中 100 折为对照组。模型既要输出每个折扣档位的绝对完单概率，也要输出相对 100 折的个体因果效应：

$$
\tau_a(X)=\mu_a(X)-\mu_{100}(X)
$$

当前工作重点是提升冒泡粒度 CATE/绝对响应仿真的可信度，而不是优化 MTAUCC、AUCC 或 Qini 排序指标。

## 2. 当前训练数据口径

### 2.1 样本与切分

- 数据范围：幸运盒子真正未开城的城市。
- Treatment：`observed_uabox_discount`，共 7 档：82、85、88、91、94、97、100。
- Label：`bcr_label_finish`。
- 输入特征：352 个。
- 已明确剔除 `city_id`：既不进入模型，也不作为评估分组字段。
- 审计字段不作为模型特征，也不作为样本筛选条件。
- 总行数：643,038。
- 训练集：451,188。
- 校准集：95,614。
- 测试集：96,236。
- 切分方式：`crc32(str(passenger_id)) % 20` 固定哈希；训练/校准/测试约为 70%/15%/15%，避免同一乘客跨集合泄漏。

### 2.2 Treatment 覆盖

训练集各档样本量：

| 折扣 | 样本量 | 占训练集比例 |
|---:|---:|---:|
| 82 | 65,070 | 14.42% |
| 85 | 64,757 | 14.35% |
| 88 | 63,982 | 14.18% |
| 91 | 64,033 | 14.19% |
| 94 | 62,977 | 13.96% |
| 97 | 63,008 | 13.96% |
| 100 | 67,361 | 14.93% |

七个档位覆盖均衡，没有明显的 Treatment positivity 问题。当前程序仍使用学习得到的条件 propensity，而不是强行替换成实验名义分配概率，以吸收长期 PID 分流和历史行为反馈造成的轻微实际偏差。

## 3. 另一份完单率与供需关联分析

来源 notebook：

```text
/ofs/pltf-sub-and-pap/keke/uabox_unopened_coverage_analysis/unopened_city_supply_demand_analysis.ipynb
```

该分析直接读取 `kflower_strategy.plft_v4_training_sample`，限定日期为 2026-07-08 至 2026-07-21，并过滤：

- `is_rct = 1`；
- `actual_uabox_discount IN (82,85,88,91,94,97,100)`；
- `is_uabox_finish IN (0,1)`；
- 城市属于真正未开城城市集合。

结果变量明确为 `is_uabox_finish`。分析覆盖 640,473 条样本、46,168 个正样本，整体特价盒子完单率为 7.208%。统计粒度不是冒泡级回归，而是先聚合到“城市 × 日期 × UABOX 实验臂”，共 23,071 个单元。

### 3.1 城市差异

- 共覆盖 263 个城市。
- 样本量不少于 1,000 的 136 个城市覆盖 92.5% 的样本。
- 在这 136 个主要城市中，完单率中位数为 6.32%，P10 为 1.79%，P90 为 15.16%。
- 若对全部 263 个城市不加权统计（包含小样本城市），中位数为 3.96%，P10 为 0%，P90 为 13.32%。

这说明基础完单概率存在显著的地域异质性。该结果并不直接证明 `city_id` 应进入 CATE 模型：更可泛化的做法是利用供需、履约和城市统计特征解释地域差异，避免模型记忆小城市 ID。当前 352 个特征中仍保留了城市供需、履约和历史统计特征。

### 3.2 正向关联

对每个供需特征分别建模，并吸收城市、日期和实验臂固定效应后，特征每增加 1 个加权标准差，完单率的统计关联约为：

- 自动范围活跃司机数：+2.68 个百分点。
- 自动范围空闲司机数：+2.27 个百分点。
- 近 10 分钟完单数：+2.06 个百分点。
- 近 10 分钟应答数：+2.01 个百分点。

司机供给和近期履约能力是解释基础完单率最稳定的正向信号。

### 3.3 负向关联

- 近 10 分钟冒泡数：-1.96 个百分点。
- 近 10 分钟呼叫数：-1.31 个百分点。
- 城市 28 天平均接驾距离：-1.31 个百分点。

需求堆积但供给没有同步增长时，完单率显著下降。相比城市绝对订单规模，历史 CR、AR、接驾距离等供需效率指标更有解释力。

### 3.4 解读边界

- 以上是条件统计关联，不是这些变量对完单的因果效应。
- 各指标采用单独模型估计，系数不能直接相加。
- 标准误按城市聚类，显著性经过 Benjamini–Hochberg 多重检验校正。
- 分析发生在“城市 × 日期 × 实验臂”聚合层面，适合城市诊断和候选特征筛选，不等价于冒泡级关系，也不能直接生成个体策略阈值。
- 近 10 分钟平均应答时长覆盖率仅 27.5%；竞争 AR/CR 覆盖率约 63%。缺失模式可能影响结论，建模时需要“数值填充 + 缺失标记”，不能把填充值直接解释为真实业务值。
- 这份分析主要检验哪些变量能解释结果 $Y$，更接近 nuisance outcome 模型 $m(X)=E[Y\mid X]$ 的充分性，不能直接说明哪些变量能解释 CATE。

## 4. 两份数据的目标与入样口径差异

当前模型元数据中，`bcr_label_finish` 的正样本数为：

- 训练集：160,359。
- 校准集：33,687。
- 测试集：34,441。
- 合计：228,487 / 643,038，正样本率为 **35.532%**。

而 notebook 中 `is_uabox_finish` 为 46,168 / 640,473，即 **7.208%**。阅读 SQL 后可以确认，这不是同一份数据上的计算矛盾，而是两条不同的数据链路：

| 项目    | 供需关联 notebook             | R-learner 训练集                            |
| ----- | ------------------------- | ---------------------------------------- |
| 源表    | `plft_v4_training_sample` | `plft_sim_single_uabox_rct_bcr_training` |
| Label | `is_uabox_finish`         | `bcr_label_finish`                       |
| 入样    | 原始 RCT、七档位、合法 Label、未开城   | 目标联合 RCT 的“合格冒泡”与完整特征上下文内连接后入表           |
| 行数    | 640,473                   | 643,038                                  |
| 正样本率  | 7.208%                    | 35.532%                                  |
| 分析粒度  | 城市 × 日期 × 实验臂聚合           | 冒泡级训练                                    |

训练表的字段注释将 `bcr_label_finish` 定义为“冒泡到完单标签 Y”，但其上游 `plft_sim_single_uabox_rct_fact` 的具体资格逻辑不在当前训练表 SQL 中。35.532% 与全量 RCT 冒泡的 7.208% 差距很大，最可能由合格冒泡分母和上游筛选链路造成，而不是 notebook 算错。

因此需要确认的是**目标对齐**，而不是简单要求两个数相等：

1. 仿真系统希望模拟的是所有 RCT 冒泡上的 `is_uabox_finish`，还是“合格冒泡”条件下的 `bcr_label_finish`？
2. 线上推理总体是否与 `plft_sim_single_uabox_rct_fact` 的合格冒泡总体完全一致？
3. 上游资格是否受到 Treatment 或 Treatment 之后变量影响；若是，则可能产生选择偏差。
4. `bcr_label_finish` 是否逐行等价于同一冒泡的 `is_uabox_finish`，差异是否只来自入样分母。

若线上只对同口径“合格冒泡”调用模型，35.532% 可以是合理的条件完单率；若目标是模拟所有冒泡的特价盒子完单概率，就需要重新对齐训练总体和 Label。

## 5. 基线模型架构与实验变体

### 5.1 独立 Treatment 头基线

当前模型为多 Treatment R-learner：

- 一个 outcome nuisance 网络估计 $m(X)=E[Y\mid X]$。
- 一个多分类 propensity 网络估计 $e_a(X)=P(T=a\mid X)$。
- 5 折 passenger 固定哈希交叉拟合 nuisance 和 propensity，主 CATE 网络使用 OOF 预测训练，降低同样本过拟合偏差。
- 主干宽度：256、128。
- 每个 Treatment 响应头宽度：32。
- Dropout：0.10。
- Batch size：2,048。
- 主训练最多 20 个 epoch，early stopping patience 为 5。
- checkpoint 依据校准集的联合分数选择，综合 R-loss、绝对响应误差和 CATE calibration。
- 最优 checkpoint 选出后，使用独立 calibration 集对每个档位做正斜率 Platt calibration。
- 最终 test 集只在训练和模型选择结束后评估一次。

### 5.2 Treatment 头实验序列

除 Treatment 头以外，以下实验均保持 352 个输入特征、固定 passenger 哈希切分、随机种子 1111、OOF nuisance、learned propensity、R-loss、绝对响应恢复、校准流程和 checkpoint 选择口径一致：

| 版本 | Treatment 头 | 参数量 | 状态 |
|---|---|---:|---|
| 基线 | 每档独立宽度 32 的响应头 | 153,159 | 已完成 |
| 低秩共享头 | rank=4 共享基底 + 档位载荷 | 128,391 | 已完成 |
| 低秩残差头 | rank=4 共享基底 + 每档宽度 8 的小残差头 | 135,790 | 已实现并启动，结果待回收 |

三个版本都保留在同一工程中，通过配置中的 `head_type` 独立启用；旧 checkpoint 的加载方式保持兼容。完整实验设计与结果集中记录在第 10.3 节。

## 6. 基线实验结果：`city_id` 消融

### 6.1 完成状态

截至 2026-09-14 16:33：

- 删除 `city_id` 的 352 特征模型已经训练完成。
- 主模型在第 8 个 epoch 触发 early stopping，最终选择第 3 个 epoch 的 checkpoint。
- `best.pt` 于 14:03 生成，最终测试结果于 14:51 生成。
- `response_calibration.json`、`test_metrics.json`、`test_cate_calibration.csv` 和 `test_response_calibration.csv` 均已产出。
- 原训练进程 PID `978337` 已正常退出。

### 6.2 删除 `city_id` 的最终测试对比

以下比较使用独立 test 集，且以较小为优：

| 指标 | 353 特征（保留 `city_id`） | 352 特征（删除 `city_id`） | 相对变化 |
|---|---:|---:|---:|
| CATE calibration RMSE macro | 0.018640 | **0.017173** | **改善 7.87%** |
| R-loss | 0.194982 | **0.194389** | 改善 0.30% |
| 绝对响应 arm MAE | 0.003147 | **0.003105** | 改善 1.32% |
| Factual Brier | 0.194872 | **0.194342** | 改善 0.27% |
| Factual Logloss | 0.569387 | **0.568263** | 改善 0.20% |
| Factual 绝对响应 arm MAE | **0.003856** | 0.003995 | 下降 3.59% |
| Propensity Logloss | 1.946034 | 1.946035 | 基本不变 |

最终 test 上，删除 `city_id` 后，整体 CATE 校准误差下降 7.87%，同时 R-loss、全局绝对响应误差、Brier 和 Logloss 均有小幅改善。唯一明确退化的是 factual 绝对响应 arm MAE，增加了 3.59%。因此，当前结果整体更支持采用不含 `city_id` 的模型，但不能据此认为所有折扣档位都得到改善。

### 6.3 分折扣 CATE 结果

| 折扣 | 保留 `city_id` | 删除 `city_id` | 判断 |
|---:|---:|---:|---|
| 82 | 0.018145 | **0.016806** | 改善约 7.4% |
| 85 | **0.011927** | 0.012993 | 下降约 8.9% |
| 88 | 0.017406 | **0.016403** | 改善约 5.8% |
| 91 | **0.016920** | 0.020696 | 下降约 22.3% |
| 94 | 0.027117 | **0.018862** | 改善约 30.4% |
| 97 | 0.020323 | **0.017278** | 改善约 15.0% |

82、88、94、97 折得到改善，85、91 折出现退化，其中 91 折需要重点诊断。此前校准集上曾观察到删除城市后 CATE 变差，但这一现象没有在最终独立 test 集上延续；模型结论应以预留 test 结果为主，同时通过时间外测试确认稳定性。

训练日志：

```text
/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/artifacts/logs/uabox_rlearner_unopened_352_no_city.log
```

模型输出目录：

```text
/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/artifacts/uabox_rlearner_unopened_352_no_city
```

## 7. DR 残差异质性分析

### 7.1 分析目标

在独立 test 集上，对每个非对照档位构造 DR 伪效应：

$$
\begin{aligned}
\psi_a(X)
&=\hat\mu_a(X)-\hat\mu_{100}(X) \\
&\quad+\frac{\mathbb I(T=a)}{\hat e_a(X)}\bigl(Y-\hat\mu_a(X)\bigr) \\
&\quad-\frac{\mathbb I(T=100)}{\hat e_{100}(X)}\bigl(Y-\hat\mu_{100}(X)\bigr)
\end{aligned}
$$

并计算：

$$
r_a=\psi_a(X)-\hat\tau_a(X)
$$

若 $r_a$ 仍能被特征稳定预测，说明当前 CATE 网络遗漏了可学习的异质性；若残差在折外不可预测，继续增加网络复杂度的收益有限。

### 7.2 已实现的诊断

- 352 个特征的单特征十分位残差均值、标准误和 95% 置信区间。
- 按 passenger_id 固定哈希的 5 折浅层树；只依据留出折 R² 和 MSE 增益判断残差可预测性。
- 统计跨折稳定进入树分裂的特征，避免依据单棵树的重要性作结论。
- 预先指定三组交互：
  - 司机供给 × 用户活跃度。
  - 价格 × 距离。
  - 用户活跃度 × 时段。
- 交互分析扣除两个变量各自主效应，观察纯交互残差。

### 7.3 第一次运行失败记录（历史过程）

- 分析程序：`scripts/analyze_dr_residual_heterogeneity.py`。
- 等待进程 PID `1104173` 已在主训练完成后启动分析，目前进程已经退出。
- 残差分析没有成功完成，结果目录中尚未生成分析文件。
- 报错发生在浅层树交叉验证阶段：某个 held-out fold 的测试样本数为 0，`DecisionTreeRegressor.predict` 无法处理形状为 `(0, 352)` 的输入。
- 当前代码缺少空 fold 防护。重新运行前，需要先检查 `passenger_id` 的读取与哈希 fold 分布，并对空训练/测试 fold 做显式检查；不能把本次异常退出解释为“残差不可预测”。

结果目录：

```text
/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/artifacts/uabox_rlearner_unopened_352_no_city/dr_residual_heterogeneity_test
```

修复并重新运行后计划输出：

- `REPORT.md`：自动汇总报告。
- `overall_residual_bias.csv`：各档位全局残差偏差。
- `feature_quantile_residuals.csv`：所有单特征分箱明细。
- `feature_quantile_ranking.csv`：单特征残差解释强度排序。
- `tree_fold_metrics.csv`：浅层树折外 R² 与 MSE 增益。
- `tree_feature_stability.csv`：跨折稳定特征。
- `tree_rules.txt`：树分裂规则。
- `interaction_summary.csv`：预设交互汇总。
- `interaction_cells.csv`：交互网格明细。

### 7.4 修复后的最终运行结果（已完成）

上述 7.3 记录的是第一次运行失败时的状态。随后已经确认根因：test 集本身使用 `crc32(passenger_id) % 20` 切分，而残差程序又复用 CRC32 做 `% 5`，两次取模并不独立，导致 5 个诊断 fold 的样本量为：

```text
[31820, 0, 0, 32013, 32403]
```

程序现已改成带独立命名空间的稳定 BLAKE2 哈希，并继续保证同一 passenger 只进入一个 fold。修复后 5 个 fold 的样本量为：

```text
[19536, 18776, 19716, 19093, 19115]
```

完整分析已于 2026-09-14 成功完成，共覆盖 96,236 条独立 test 样本和 27,788 个 passenger。日志最终状态为：

```json
{"status":"complete","rows":96236}
```

#### 7.4.1 全局 DR 残差偏差

| 折扣 | 残差均值 | 标准误 | 判断 |
|---:|---:|---:|---|
| 82 | 0.006831 | 0.005321 | 不显著 |
| 85 | 0.004791 | 0.005170 | 不显著 |
| 88 | 0.008735 | 0.005352 | 边缘但不显著 |
| 91 | -0.001405 | 0.005254 | 不显著 |
| 94 | 0.001743 | 0.005299 | 不显著 |
| 97 | 0.001756 | 0.005329 | 不显著 |

各档位残差均值都没有显著偏离 0。85 折可能存在约 0.48 个百分点的平均低估，但其标准误约为 0.52 个百分点，证据不足；91 折的平均残差接近 0，主要问题不是 ATE 偏差。

#### 7.4.2 浅层树折外残差预测能力

| 折扣 | 平均 R² | 相对 MSE 增益 | 正增益 fold |
|---:|---:|---:|---:|
| 82 | -0.000466 | -0.0393% | 1/5 |
| 85 | -0.001093 | -0.0938% | 0/5 |
| 88 | -0.001146 | -0.1121% | 0/5 |
| 91 | -0.001201 | -0.1179% | 0/5 |
| 94 | -0.000615 | -0.0507% | 0/5 |
| 97 | -0.001315 | -0.1198% | 0/5 |

所有档位的平均 MSE 增益都是负数。85、91 折更是 5 个 passenger 留出 fold 全部没有正收益，说明当前特征无法稳定预测剩余 DR 残差。

#### 7.4.3 单特征与交互结果

没有任何特征能够稳定进入多数 fold 的树分裂。单特征分箱中虽然出现了一些较大的样本内残差波动，例如：

- 85 折：城市历史履约能力、城市订单量。
- 91 折：城市小时呼叫量、历史应答量、价格档位特征。
- 其他档位：城市历史完单率、ECR、价格和用户活跃度特征。

但这些模式没有形成折外预测收益，更像多重比较与 DR 高方差产生的偶然波动，不能据此加回 `city_id` 或增加对应网络结构。

样本内较大的预设交互包括价格 × 距离、活跃度 × 时段以及供给 × 用户活跃度。部分价格 × 距离 cell 的残差甚至超过合理的概率效应范围，说明结果明显受到稀疏 cell 与 DR 噪声影响。由于浅层树的折外增益为负，这些交互目前不具备模型改造依据。

#### 7.4.4 对模型改造的含义

1. 当前没有证据支持继续加深 CATE 网络。
2. 不建议依据单次分箱结果批量增加显式交互。
3. 不建议重新加入离散 `city_id`。
4. 85 折可以继续观察平均效应偏差，但目前不足以证明需要强制修正。
5. 91 折可以针对 CATE 输出尺度做轻度正则或 calibration，但应通过多随机种子和时间外测试确认。
6. 下一阶段更可能从多种子 ensemble、时间外验证、Label 口径确认和降低估计方差中获得收益，而不是增加模型容量。

最终结果目录：

```text
/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/artifacts/uabox_rlearner_unopened_352_no_city/dr_residual_heterogeneity_test
```

## 8. DR 残差结果的判断标准与决策规则

残差分析完成后，不能只看某个分箱的残差绝对值，需要同时满足：

1. 浅层树在留出 passenger 折上的相对 MSE 增益稳定为正。
2. 同一特征在多数折、多个相邻折扣档位中重复出现。
3. 残差方向和业务机制一致，而不是只由极端值或小样本分箱造成。
4. 残差幅度对冒泡级仿真具有实际意义。
5. propensity 没有极端值，DR 残差不是由少量大权重样本主导。

对应决策：

- 若供需 × 用户、价格 × 距离等交互在折外稳定解释残差，优先增加显式交互、门控或分支结构。
- 若只有全局残差均值偏离零，先处理绝对响应校准或 ATE 偏差，不必直接扩大 CATE 网络。
- 若单特征分箱有波动、但浅层树折外增益接近零，则更可能是 DR 伪标签噪声，不建议增加模型容量。
- 若残差不可预测，应优先投入 Label 口径、数据质量、时间外验证和仿真校准，而不是继续加深网络。

## 9. 历史阶段性结论（DR 修复前）

> 本章完整保留当时的阶段性判断，其中第 3 条所述“DR 残差分析异常退出”后来已经修复并完成。当前有效结论请以第 7.4 节和第 11 章为准。

1. 未开城样本的七档 Treatment 覆盖均衡，具备训练多档位响应模型的基础。
2. 去掉离散 `city_id` 是合理的泛化约束，但供需、履约和城市统计特征仍需保留，用于解释基础响应和可迁移的地域环境差异。
3. 完单率分析证明供需环境对 $Y$ 很重要，但尚未证明它会导致折扣 CATE 异质性；DR 残差分析本次因空 fold 异常退出，修复后才能回答该问题。
4. 删除 `city_id` 的模型已经完成训练和独立测试；整体 CATE calibration RMSE 改善 7.87%，当前优先保留 352 特征版本。
5. 分档效果并不一致：85、91 折 CATE 退化，尤其需要排查 91 折的样本、校准稳定性和残差异质性。
6. 最大风险不是两个完单率数值不同本身，而是仿真目标总体是否与训练表的“合格冒泡”总体一致。交付前应补齐 `plft_sim_single_uabox_rct_fact` 的资格定义，并验证该资格不是 Treatment 之后的选择变量。

## 10. CATE 提升行动、架构实验与后续方案

### 10.1 DR 残差异质性分析修复计划（已完成，保留历史记录）

> 本节保留修复前的计划与问题描述；实际根因、修复方式和最终结果见第 7.4 节。

这是当前最优先的工作。需要检验：

$$
r_a=\psi_a-\hat\tau_a(X)
$$

是否还能被供需、用户、价格、距离、时段等特征稳定预测。

- 如果浅层树在留出乘客上的 MSE 明显下降，说明当前模型遗漏了可学习的 CATE 结构。
- 如果只有 85、91 折的残差可预测，可以只对这两个 Treatment 分支做针对性改造。
- 如果残差在折外不可预测，继续加深网络大概率只会拟合 DR 伪标签噪声。

当前分析因空 fold 报错，必须先检查 `passenger_id` 的读取和哈希 fold 分布，并补充空训练集/测试集防护，再重新运行。

### 10.2 85、91 折专项诊断（已完成）

删除 `city_id` 后整体 CATE 改善，但 85、91 折下降，尤其 91 折下降约 22%。需要分别检查：

- 各人群、供需状态和时间段内的 DR calibration。
- 预测 CATE 的方差是否过大，或者是否发生过度收缩。
- Calibration slope 是否明显偏离 1。
- 91 vs 100、85 vs 100 的局部实验分配是否仍然均衡。
- 两个档位的正样本数量、Label 质量和 propensity 权重尾部。

不建议因为两个档位退化就直接重新加入 `city_id`；可以先对对应 Treatment 头调整容量和正则化强度。

#### 10.2.1 专项诊断结论

诊断使用删除 `city_id` 后的 352 特征模型，在独立 test 集上完成。test 集包含 96,236 条样本和 27,788 个 passenger，以 100 折为对照组。

主要结论：

1. 85、91 折均没有样本不足、Label 异常、propensity 极端权重或明显全局协变量失衡。
2. 85 折的 CATE dispersion 基本合理，主要问题是平均效应低估，不适合直接增强收缩。
3. 91 折的平均效应已经准确，但 calibration slope 仍小于 1，说明预测的个体差异振幅偏大，适合增加档位头收缩或单独做 CATE scale calibration。
4. 删除 `city_id` 后，91 折 RMSE 虽然变差，但 slope 从 0.377 改善到 0.703，因此不是全面退化；RMSE 上升主要由若干高噪声校准分箱造成。
5. 5 折浅层树对 85、91 折 DR 残差的折外 MSE 增益全部为负，目前没有证据支持增加网络容量或加入大规模交互。

#### 10.2.2 样本与 Label

| Treatment | 样本量 | 正样本 | 完单率 |
|---:|---:|---:|---:|
| 85 | 13,763 | 4,951 | 35.9733% |
| 91 | 13,579 | 4,851 | 35.7243% |
| 100 | 14,428 | 4,986 | 34.5578% |

原始实验 ATE：

- 85 vs 100：1.4155 个百分点。
- 91 vs 100：1.1665 个百分点。

两档正样本均接近 5,000，没有明显的稀疏 Label 问题。

#### 10.2.3 CATE 均值、方差与校准

85 折：

- 预测 CATE 均值：0.7486 个百分点。
- DR ATE：1.2277 个百分点。
- 全局 DR 残差：0.4791 ± 0.5170 个百分点，未显著偏离 0。
- CATE 标准差：1.6526 个百分点。
- CATE P01/P50/P99：-2.6460% / 0.6531% / 4.7487%。
- 官方分箱 calibration slope：0.9799。
- DR 分箱 slope：0.8615。

85 折的 slope 接近 1，预测异质性振幅没有明显过度扩张或收缩。更突出的问题是平均效应偏低，因此不建议对整个 85 折响应头继续加强 shrinkage，应优先处理档位均值或截距校准。

91 折：

- 预测 CATE 均值：1.2268 个百分点。
- DR ATE：1.0863 个百分点。
- 全局 DR 残差：-0.1405 ± 0.5254 个百分点，未显著偏离 0。
- CATE 标准差：1.2871 个百分点。
- CATE P01/P50/P99：-1.1740% / 1.0637% / 4.5579%。
- 官方分箱 calibration slope：0.7028。
- DR 分箱 slope：0.6743。

91 折均值准确，但 slope 明显小于 1，表示模型预测的冒泡间差异振幅偏大，而不是过度收缩。可以对 91 折 CATE 头增加更强正则，或在 calibration 集上学习正斜率缩放：

$$
\tau_{91}^{\mathrm{cal}}(X)
=\alpha_{91}+\beta_{91}\hat\tau_{91}(X),
\qquad 0<\beta_{91}<1
$$

#### 10.2.4 与保留 `city_id` 模型的比较

| 档位 | 指标 | 保留城市 | 删除城市 |
|---:|---|---:|---:|
| 85 | CATE RMSE | 0.011927 | 0.012993 |
| 85 | slope | 0.9935 | 0.9799 |
| 85 | CATE std | 0.01622 | 0.01653 |
| 91 | CATE RMSE | 0.016920 | 0.020696 |
| 91 | slope | 0.3770 | 0.7028 |
| 91 | CATE std | 0.01317 | 0.01287 |

91 折删除城市后，RMSE 变差约 22%，但 slope 明显改善，ATE 误差也仍然只有约 0.00060。test 中每个 CATE 十分位约 2,800 条样本，而单个分箱的 DR 标准误约为 0.016–0.017，已经大于预测 CATE 的标准差。因此几个分箱的随机波动就足以改变 RMSE，不能仅凭一次切分认定 `city_id` 提供了真实的个体效应信息。

#### 10.2.5 实验分配与协变量平衡

全 352 个特征的最大绝对标准化均值差：

- 85 vs 100：0.0260；没有特征超过 0.05。
- 91 vs 100：0.0310；没有特征超过 0.05。

这说明两组在可观测特征上平衡良好，没有发现足以解释 CATE 退化的系统性分配偏差。

部分供需、距离和时间分组的 Treatment 占比与 propensity 预测存在约 1–2 个百分点差异，但方向并不稳定，而且全局 SMD 很小。更可能是 learned propensity 的轻微概率校准偏差，而不是严重的 RCT 随机化破坏。

#### 10.2.6 Propensity 与权重尾部

85 vs 100：

- Pair propensity P01/P50/P99：0.4620 / 0.5060 / 0.5289。
- 实际分配概率对应的逆权重最大值：2.723。
- 85 折 ESS：13,752.8 / 13,763。
- 100 折 ESS：14,418.1 / 14,428。

91 vs 100：

- Pair propensity P01/P50/P99：0.4497 / 0.4972 / 0.5155。
- 实际分配概率对应的逆权重最大值：3.059。
- 91 折 ESS：13,570.7 / 13,579。
- 100 折 ESS：14,419.9 / 14,428。

权重非常稳定，ESS 几乎等于原始样本量。当前问题不是 propensity 极端值或少量大权重样本主导。

#### 10.2.7 DR 残差可预测性

| 档位 | 浅层树平均 R² | 相对 MSE 增益 | 正增益 fold |
|---:|---:|---:|---:|
| 85 | -0.001093 | -0.0938% | 0/5 |
| 91 | -0.001201 | -0.1179% | 0/5 |

虽然单特征分箱中能看到城市历史履约、价格、距离和时段的残差波动，但这些模式没有在 passenger 留出折上形成正向预测增益。91 折也没有任何特征稳定进入多数折的树分裂。

因此当前不应根据单次分箱结果增加大规模交互或扩大网络。价格 × 距离、活跃度 × 时段在样本内看起来波动较大，但目前缺少折外稳定证据，且部分极端 cell 很可能来自小样本与 DR 噪声。

#### 10.2.8 对应建议

1. 保持删除 `city_id`，暂不恢复离散城市特征。
2. 85 折优先做档位均值或截距校准，不加强整体 CATE 收缩。
3. 91 折尝试 head-specific L2、较小 head width，或单独的正斜率 CATE scale calibration，使个体效应振幅向约 0.7 倍收缩。
4. Learned propensity 不直接替换为名义分配概率；在 calibration 集上做概率校准，或验证条件 propensity 与经验 arm probability 的收缩混合。
5. 用至少 3–5 个模型随机种子和时间外 test 重复比较 85、91 折。只有 RMSE 退化稳定复现后，才把它视为结构性问题。
6. 当前不建议增加更深的 CATE 网络或批量显式交互，因为 DR 残差在 5 个 passenger 留出折上均不可预测。

远程完整报告：

```text
/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/artifacts/uabox_rlearner_unopened_352_no_city/diagnosis_85_91.md
```

### 10.3 Treatment 头结构实验

#### 10.3.1 候选方案：有序剂量结构（待验证）

折扣档位天然有序：

$$
82<85<88<91<94<97<100
$$

当前每个 Treatment 使用独立响应头，无法充分共享相邻折扣档位的信息。可以改写为：

$$
\mu_a(X)=\mu_{100}(X)+\sum_{k:a\leq k<100}\Delta_k(X)
$$

其中，$\Delta_k(X)$ 表示相邻价格档位之间的增量效应。这样可以：

- 共享相邻档位的信息，降低单档估计方差。
- 提高折扣响应曲线的平滑性。
- 减少 85、91 折这类单独响应头的不稳定性。
- 可选地加入软单调约束。

单调约束不宜设置得过强，因为真实完单响应未必严格单调。更适合使用 soft monotonic penalty，而不是硬截断模型输出。

#### 10.3.2 已完成实验：共享低秩 Treatment 头（rank=4）

为了验证“档位之间共享信息”是否能提升冒泡粒度 CATE，本轮只替换 Treatment 响应头，保持数据、352 个特征、固定 passenger 哈希切分、随机种子 1111、OOF nuisance、learned propensity、R-loss、绝对响应恢复、校准和 checkpoint 选择口径不变。原独立头实验及模型文件均未覆盖。

低秩头使用共享非线性基底与档位载荷：

$$
q(X)=B(z(X))\in\mathbb{R}^{4},
\qquad
\ell_a(X)=b_a+w_a^{\mathsf T}q(X),
$$

$$
\widetilde\mu_a(X)=\sigma\!\left(\ell_a(X)\right),
\qquad
\widehat\tau_a(X)=\widetilde\mu_a(X)-\widetilde\mu_{100}(X).
$$

其中，$z(X)$ 是原共享编码器输出，$q(X)$ 是 rank=4 的共享响应基底，$w_a$ 和 $b_a$ 是每个折扣档位的载荷与偏置。训练与推理时的最终绝对响应概率仍按照原有 nuisance response 和 propensity 恢复，未改成新的 baseline-response 分解。本次改造使模型总参数量从 153,159 降至 128,391，减少约 16.2%。

实验配置与结果目录：

```text
配置：/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/configs/uabox_rlearner_no_city_low_rank.yaml
结果：/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/artifacts/uabox_rlearner_unopened_352_no_city_low_rank_r4
日志：/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/artifacts/logs/uabox_rlearner_unopened_352_no_city_low_rank_r4.log
```

训练在 epoch 11 触发 early stopping，验证集综合最优 checkpoint 为 epoch 6：

| 验证集指标 | epoch 6 |
|---|---:|
| checkpoint score | 0.217033 |
| CATE calibration RMSE macro | 0.015854 |
| absolute response arm MAE | 0.004072 |
| validation R-loss | 0.193034 |

最终测试集与原“删除 `city_id`、独立 Treatment 头”模型的同口径比较如下：

| 测试集指标 | 独立头 | 低秩共享头 | 相对变化 |
|---|---:|---:|---:|
| CATE calibration RMSE macro | 0.017173 | 0.017150 | 改善 0.14% |
| absolute response arm MAE | 0.003105 | 0.003085 | 改善 0.65% |
| factual absolute response arm MAE | 0.003995 | 0.003995 | 基本不变 |
| R-loss | 0.194389 | 0.194344 | 基本不变 |

分档 CATE calibration RMSE：

| 档位 | 独立头 | 低秩共享头 | 相对变化 |
|---:|---:|---:|---:|
| 82 | 0.016806 | 0.016275 | 改善 3.2% |
| 85 | 0.012993 | 0.017447 | 下降 34.3% |
| 88 | 0.016403 | 0.015107 | 改善 7.9% |
| 91 | 0.020696 | 0.019826 | 改善 4.2% |
| 94 | 0.018862 | 0.019830 | 下降 5.1% |
| 97 | 0.017278 | 0.014414 | 改善 16.6% |

分档 calibration slope：

| 档位 | 独立头 | 低秩共享头 | 低秩头解释 |
|---:|---:|---:|---|
| 82 | 0.902 | 0.728 | 预测异质性幅度偏大 |
| 85 | 0.980 | 0.777 | 预测异质性幅度偏大 |
| 88 | 1.284 | 1.210 | 预测异质性略偏小 |
| 91 | 0.703 | 1.552 | 预测异质性偏小 |
| 94 | 0.497 | 0.066 | 分箱效应与预测幅度关系很弱 |
| 97 | 0.338 | 0.145 | 分箱效应与预测幅度关系较弱 |

实验结论：低秩共享头在 82、88、91、97 折上降低了 CATE calibration RMSE，但明显损害 85 折，94 折也有小幅退化；整体 macro RMSE 仅改善 0.14%，验证集上的明显改善没有等比例迁移到测试集。因此，本轮结果不能证明 rank=4 的完全共享低秩头能够稳定提升冒泡级 CATE，暂不应直接替换原独立头模型。

该结果仍说明档位间共享可能有价值，但固定 rank=4 的完全共享约束会让不同档位互相牵制。若继续沿这个方向实验，更合适的结构是“共享低秩部分 + 每档位小型残差头”：共享部分学习跨档位共性，残差头为 85、94 等响应模式不同的档位保留有限的独立修正能力。该方案需要继续采用相同切分、相同 OOF nuisance、相同 seed，并至少补充多随机种子和时间外测试后再判断。

#### 10.3.3 进行中实验：共享低秩部分 + 每档位小型残差头

为缓解纯低秩头对所有档位施加相同共享约束的问题，已经实现以下混合结构：

$$
\ell_a(X)=b_a+w_a^{\mathsf T}q(X)+r_a(X)
$$

其中，$q(X)$ 仍是 rank=4 的跨档位共享基底，$r_a(X)$ 是每个档位独立、宽度为 8 的小型残差头。残差头最后一层使用全零初始化，因此训练开始时与纯低秩结构一致；只有某一档位存在可重复的独立信号时，对应 $r_a(X)$ 才逐步偏离 0。该结构既保留跨档位共性，也允许 85、94 折学习自己的有限修正。

实验配置与目录：

```text
配置：/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/configs/uabox_rlearner_no_city_low_rank_residual.yaml
结果：/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/artifacts/uabox_rlearner_unopened_352_no_city_low_rank_r4_residual_w8
日志：/ofs/pltf-sub-and-pap/keke/uabox_rlearner_model/artifacts/logs/uabox_rlearner_unopened_352_no_city_low_rank_r4_residual_w8.log
```

该版本总参数量为 135,790，介于纯低秩头和独立头之间。代码、推理入口、DR 残差分析入口和测试均已适配，8 项模型测试通过；正式训练已经启动，最终结果尚待回收。对比时必须继续使用最终独立 test 集，并重点观察 85、94 折是否修复，同时确认 82、88、91、97 折的已有收益没有被残差自由度抵消。

### 10.4 待验证：拆分基础响应与 Treatment 效应修正

下一版可以使用以下绝对响应结构：

$$
\operatorname{logit}\mu_a(X)=g(X)+h_a(X)
$$

其中：

- $g(X)$ 学习用户、供需、距离、履约等因素决定的基础完单概率。
- $h_a(X)$ 只学习 Treatment 相对于基础响应造成的变化。
- Treatment 分支使用较小网络、低秩结构或有序增量头。

这种拆分能够避免供需特征对基础完单率的强解释能力淹没较弱的 Treatment 异质性。对于当前约 35.5% 的训练 Label 正样本率，在 logit 空间组合通常也比在概率空间直接相加更稳定。

### 10.5 条件性方案：根据残差结果增加针对性显式交互

只有当 DR 残差分析确认某类交互具有稳定的折外解释能力时，才建议加入模型，例如：

- 司机供给状态 × 用户活跃度。
- 价格或预估费用 × 行程距离。
- 接驾距离 × 折扣。
- 供需紧张程度 × 折扣。
- 用户价格敏感度 × 折扣。
- 用户活跃度 × 时段。

实现上优先考虑小型门控网络、低秩交互层或少量人工确认的交叉特征，不建议对 352 个特征做全量二阶交叉，否则容易显著增加过拟合风险。

## 11. 当前有效结论与实验状态

1. 当前业务目标是冒泡粒度的绝对响应和 CATE 仿真，不以 MTAUCC、AUCC 或 Qini 为主要优化目标。
2. 未开城样本七档 Treatment 覆盖均衡，暂未发现严重 positivity、局部分配失衡或 propensity 权重尾部问题。
3. 当前优先保留删除 `city_id` 的 352 特征版本；相较保留城市的版本，其最终 test CATE calibration RMSE macro 改善 7.87%。
4. 完整 DR 残差分析已经修复并完成。所有档位的浅层树折外平均 MSE 增益均为负，现阶段没有证据支持加深整体 CATE 网络或批量加入显式交互。
5. 85、91 折的专项诊断没有发现样本量、Label、RCT 平衡或极端权重问题。85 折更接近均值偏差，91 折更接近预测异质性振幅偏大；两者都需要多随机种子和时间外测试确认。
6. rank=4 纯低秩共享头已经完成。最终 test CATE calibration RMSE macro 仅从 0.017173 改善到 0.017150，整体改善 0.14%，但 85 折下降 34.3%、94 折下降 5.1%，不足以替换独立头基线。
7. “rank=4 共享基底 + 每档宽度 8 残差头”已经实现并启动，目标是保留共享收益，同时让 85、94 折学习有限独立修正；结果尚未回收。
8. 在新架构得到稳定证据前，不应只依据单个 seed、单次分箱 slope 或验证集提升替换当前模型。最终判断应基于独立 test、3–5 个随机种子和时间外数据。
9. 交付前仍需确认 `bcr_label_finish`、上游合格冒泡资格和线上推理总体完全一致，并排除 Treatment 之后筛选造成的选择偏差。

## 12. CATE 评估口径、统计分辨率与验收门槛

### 12.1 为什么不存在可直接观测的“单冒泡 CATE 准确率”

对于同一个冒泡，只能观察它实际进入的一个 Treatment 对应的结果：

$$
Y=Y(T)
$$

其他折扣下的潜在结果 $Y(a)$ 都是反事实，无法在同一个冒泡上同时观测。因此，不能像普通监督学习那样逐行计算：

$$
\left(\hat\tau_a(X)-\tau_a(X)\right)^2
$$

这里的真实个体 $\tau_a(X)$ 并不存在可直接取得的 Label。单冒泡最终是否完单还是一次 Bernoulli 随机实现，即使概率预测完全正确，也不能要求每条样本的 0/1 结果都被预测正确。因此，普通分类 accuracy 不能作为冒泡级因果仿真的核心验收指标。

当前能被数据识别和检验的是：

1. 预测 CATE 从低到高分组后，各组的实验或 DR 观测效应是否随之变化。
2. 预测的异质性振幅是否与观测分组效应一致，即 calibration slope 是否接近 1。
3. 每个档位的平均效应是否准确，即 ATE error 是否足够小。
4. 各档位绝对响应概率是否校准。
5. 模型用于策略选择时，离线 policy value 或 policy regret 是否改善。
6. 以上结论是否能在不同随机种子和时间外数据上重复。

因此，本文中的“CATE 精度”实际指分组层面的可验证校准精度和策略有效性，不等价于已经知道每个冒泡的真实反事实效应。

### 12.2 当前测试集能够分辨多小的效应

当前独立 test 集有 96,236 条样本，七档 Treatment 基本均衡，每档约 13,500–14,400 条；Label 正样本率约为 35.5%。以两个档位样本量分别为 $n_a$、$n_{100}$，完单率近似为 $p$，简单两组均值差的标准误约为：

$$
\operatorname{SE}(\widehat{ATE}_a)
\approx
\sqrt{p(1-p)\left(\frac{1}{n_a}+\frac{1}{n_{100}}\right)}
$$

代入 $p\approx0.355$ 和当前每档样本量，档位整体 ATE 的标准误约为 0.5–0.6 个百分点。这个量级与当前 DR 全局残差标准误约 0.52 个百分点基本一致。

当每个档位按预测 CATE 分为 10 箱时，每箱每档通常只有约 1,300–1,500 条样本。样本量约缩小到原来的十分之一，标准误相应扩大约 $\sqrt{10}$ 倍，因此单个 CATE 分箱的效应标准误约为 1.6–1.8 个百分点。

这意味着当前观测到的分箱 CATE calibration RMSE 约 1.7 个百分点，既包含模型误差，也包含 DR 伪效应和实验均值的抽样噪声。它不能被直接解释成“模型真实的单冒泡 CATE 误差就是 1.7 个百分点”。在模型误差与分箱估计噪声近似独立的理想条件下，可以参考：

$$
\operatorname{RMSE}_{\mathrm{observed}}^2
\approx
\operatorname{RMSE}_{\mathrm{model}}^2
+
\operatorname{SE}_{\mathrm{bin}}^2
$$

但该分解依赖独立性、同方差和正确 propensity 等假设，只能作为辅助诊断，不能用它宣称已经得到真实个体误差。

### 12.3 为什么门槛要结合 Treatment 信号大小

测试集上的原始实验 ATE 大约为：

| 档位 | 相对 100 折的实验 ATE |
|---:|---:|
| 82 | 2.208 个百分点 |
| 85 | 1.415 个百分点 |
| 88 | 1.810 个百分点 |
| 91 | 1.166 个百分点 |
| 94 | 1.011 个百分点 |
| 97 | 1.060 个百分点 |

这些档位的典型平均效应只有约 1–2 个百分点，中位数量级约为 1.3 个百分点。若 CATE 校准误差也在 1.7 个百分点左右，误差与信号本身处于同一量级，模型就不能可靠区分细小的冒泡效应差异，只能用于较粗粒度的人群或总体仿真。

一个通用的业务定标方法是先定义最小有意义差异 $\delta_{\min}$：如果两个候选折扣的个体响应概率差小于该值，业务上就认为它们基本等价。然后要求：

$$
\operatorname{RMSE}_{CATE}
\leq
\frac{1}{2}\delta_{\min}
$$

如果希望更加稳健地进行逐冒泡档位选择，可以将要求提高到：

$$
\operatorname{RMSE}_{CATE}
\leq
\frac{1}{3}\delta_{\min}
$$

因此门槛并不是由 352 个特征数直接决定，而是由“业务需要区分多小的效应差异”决定。若相邻档位的真实效应只相差 0.3–0.5 个百分点，即使整体 CATE RMSE 达到 0.5 个百分点，也可能不足以可靠选择最优档位。

### 12.4 分级验收门槛

以下门槛是结合当前 1–2 个百分点的 Treatment 信号、冒泡仿真用途和统计分辨率制定的工程验收带，不是适用于所有因果模型的普遍定律：

| 使用目标 | CATE RMSE macro | 单档 CATE RMSE | 适用判断 |
|---|---:|---:|---|
| 粗粒度总体或大人群仿真 | 不高于 1.5 个百分点 | 不高于 2.0 个百分点 | 可以描述总体趋势，但不把单条 CATE 当作精确数值 |
| 供需、人群分层仿真 | 不高于 1.0 个百分点 | 不高于 1.5 个百分点 | 当前建议的基础交付线 |
| 冒泡级精细策略选择 | 不高于 0.5 个百分点 | 不高于 0.8 个百分点 | 需要更大时间外 test 和 policy value 验证 |
| 高可信个体效应数值 | 不高于 0.3 个百分点 | 不高于 0.5 个百分点 | 当前数据几乎无法直接验证 |

对于当前项目，建议将“可进入分层仿真验证”的基础门槛定为：

1. 时间外测试的 CATE calibration RMSE macro 不高于 1.0 个百分点。
2. 任一核心档位 CATE RMSE 不高于 1.5 个百分点。
3. 在预测 CATE 方差和分箱有效样本量足够时，calibration slope 位于 0.8–1.2；若置信区间很宽，则标记为不可识别，不能仅依据点估计否决模型。
4. Calibration intercept 绝对值不高于 0.3 个百分点。
5. 各档 ATE error 不高于 0.3–0.5 个百分点。
6. 绝对响应 arm MAE 不高于 0.5 个百分点。
7. 3–5 个随机种子之间，CATE RMSE macro 波动不高于 0.2–0.3 个百分点。
8. 时间外数据和供需、距离、活跃度等关键业务分层中，不出现稳定同方向的大偏差。

其中 1.0 个百分点被选为基础线，是因为它已经低于当前约 1.3 个百分点的典型 ATE 信号，能够开始区分较大的人群响应差异；但它仍不足以证明相邻档位的逐冒泡排序可靠。0.5 个百分点被定义为精细策略线，是因为它大约为典型 Treatment 效应的三分之一到二分之一，误差不再与主要信号等大，但仍必须结合 policy regret 判断。

### 12.5 当前模型处于什么水平

删除 `city_id` 的独立头模型最终 test 指标为：

- CATE calibration RMSE macro：约 1.717 个百分点。
- 各档 CATE RMSE：约 1.299–2.070 个百分点。
- 绝对响应 arm MAE：约 0.311 个百分点。

纯低秩头最终 test 指标为：

- CATE calibration RMSE macro：约 1.715 个百分点。
- 各档 CATE RMSE：约 1.441–1.983 个百分点。
- 绝对响应 arm MAE：约 0.308 个百分点。

因此，当前模型的绝对响应总体校准已经达到“不高于 0.5 个百分点”的基础线，但 CATE 分组校准仍处于粗粒度仿真水平，尚未达到建议的 1.0 个百分点分层仿真线。现阶段可以用模型模拟总体或较大人群的随机响应趋势，但不应把每条冒泡输出的 CATE 当作已验证的精确真值，也不应仅凭该值直接选择相邻折扣档位。

正在训练的“低秩共享 + 每档小型残差头”需要使用同一套门槛评价。即使某次 validation RMSE 明显下降，只有在最终独立 test、多个 seed 和时间外数据上重复，才能认为冒泡级预测能力得到真实改善。

### 12.6 若要验证更严格门槛，需要多少测试数据

在其他条件近似不变时，效应估计标准误与样本量平方根成反比：

$$
\operatorname{SE}\propto\frac{1}{\sqrt{N}}
$$

若当前 10 箱评估的分箱噪声约为 1.7 个百分点，则目标噪声为 $s_{\mathrm{target}}$ 时，测试集规模近似需要乘以：

$$
\left(\frac{1.7}{s_{\mathrm{target}}}\right)^2
$$

由此得到：

| 目标分箱标准误 | 相对当前 test 规模 | 近似独立测试样本量 |
|---:|---:|---:|
| 1.0 个百分点 | 约 2.9 倍 | 约 28–30 万 |
| 0.5 个百分点 | 约 11.6 倍 | 约 110 万 |
| 0.3 个百分点 | 约 32 倍 | 约 300 万 |

这些估算用于说明验证分辨率，不代表增加训练样本后模型误差一定按相同比例下降。若无法扩大独立 test，可以减少分箱数量、合并业务相近档位、积累更长时间的 RCT 数据或使用多期重复评估来提高统计功效；但减少分箱会降低对细粒度异质性的分辨能力。

### 12.7 最终交付时应同时报告什么

最终模型不能只交付一个 CATE RMSE，需要同时报告：

1. 每档 ATE、ATE error 及置信区间。
2. 每档 CATE calibration RMSE、slope、intercept 和分箱标准误。
3. 绝对响应 MAE、Brier、Logloss 和分组 calibration。
4. 每档预测 CATE 的均值、标准差、P01、P50、P99，检查过度扩张或塌缩。
5. Propensity 分布、逆权重尾部和 ESS。
6. 多随机种子均值、标准差与最差结果。
7. 时间外测试和关键业务分层结果。
8. 若模型用于档位决策，必须补充 policy value、policy regret，以及策略相对随机或固定档位基线的提升和置信区间。

只有当概率校准、CATE 分组校准、稳定性和策略价值同时通过，才能认为模型满足冒泡粒度仿真的预测需要。
