# PCVRHyFormer 项目笔记

这套笔记只保留可复现的模型事实、实验证据与面试表达。历史目录曾在多次试验后继续被修改，因此必须区分：**当前用于讲解的 516 前置候选感知主线**，与**有完整分数证据的历史实验结论**。

## 建议阅读顺序

1. [[当前模型架构（516 前置候选感知）]]：从输入到 logit 的完整数据流。
2. [[动态序列编码：从一条行为到 Query]]：用单个样本解释多字段行为、时间桶与前置 bilinear pooling。
3. [[Sequence Evolution：四域独立Transformer与RoPE]]：域内 Transformer、RoPE、mask 与两层序列演化。
4. [[实验复盘与证据边界]]：哪些结论有日志/源码支持，哪些仍需复验。
5. [[改动一：ID与统计值的语义对齐]]、[[改动二：请求与行为时间的双尺度对齐]]、[[改动三：前置候选感知Query初始化]]：基于 baseline 的三项核心改动。
6. [[讲解逐字稿（3分钟）]]：3 分钟主讲版本与 30 秒版本。
7. [[面试问答（完整版）]]：逐题详细作答与论文/项目口径区分。
8. [[面试深挖问题地图（从业务到落地）]]：按业务、数据、架构、实验和工程顺序组织的 42 个高频深挖问题与回答。
9. [[面试问答（精简）]]：高频追问的短答版本。
10. [[训练策略：异构优化器]]：Adagrad + AdamW 的原理与答法。

## 一页事实卡

| 项目 | 当前讲解口径 |
|---|---|
| 任务 | 单任务 PCVR 二分类，输出候选物品的转化 logit |
| 主干 | 4 路序列、2 层 HyFormer、RankMixer 全局 Token 融合 |
| 静态 Token | 3 用户 NS + 2 UE + 2 物品 NS + 1 请求时间 = 8 个，维度 64 |
| 初始 Query | 每域 2 个，共 8 个；由全部 NS Token 与本域候选感知摘要生成 |
| 候选感知 | 516 使用前置 Bilinear Target-Aware Pooling；item NS 均值作 Query |
| 时间 | 请求时段/星期周期编码 + 行为距请求的非均匀时间差分桶 |
| 训练 | BCEWithLogitsLoss；Embedding 用 Adagrad(lr=0.05)，其余参数用 AdamW(lr=1e-4, betas=(0.9,0.98))，全局梯度裁剪 1.0 |

## 证据边界

- 历史源码注释能确认约 `0.826` 是某一中间状态的参照分数，`0.8255` 是扩长序列后的回退结果。
- `516` 的前置 bilinear target-aware pooling 代码结构是自洽且可定位的；但仓库没有保留其与其他版本严格等预算对比的完整训练日志。
- 因此面试中可准确说“我采用/实现了 516 的前置候选感知结构，并将它作为当前主线”；不要把具体增益全部归因给该模块，除非补齐实验日志。
