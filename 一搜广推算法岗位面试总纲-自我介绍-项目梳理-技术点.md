---
tags:
  - 八股
  - 求职
  - 一搜广推
  - 自我介绍
  - 项目面试
  - 推荐系统
  - 广告算法
  - 搜索算法
created: 2026-07-23
---

# 一搜广推算法岗位面试总纲：自我介绍、项目梳理与技术点

目标岗位：上海—搜广推算法工程师（J101091）  
候选人：缪可可  
目标：把因果排序、行为序列、CVR 建模、召回排序和数据工程串成一条清晰主线。

> 使用方法：先熟练掌握第 1～4 节的口头表达，再按第 7 节补技术点。不要逐字背诵；每个项目记住“业务问题—Baseline—核心改动—为什么有效—结果—局限”六件事。

配套详细答案：[[一搜广推项目必答问题详细答案-Uplift-司乘序列-PCVR-HSTU]]

---

## 1. 岗位与个人匹配结论

### 1.1 岗位主要看什么

从 JD 看，岗位覆盖搜索、推荐、商业广告和工程落地四块：

1. 搜索：多路/向量召回、粗排精排、多目标排序、意图理解、Query 改写纠错、类目匹配和相关性；
2. 推荐：行为与兴趣建模、序列建模、冷启动、多样性公平、多任务学习、召回排序重排；
3. 广告：CTR/CVR、预算和出价、流量分配、归因、ROI 与用户体验；
4. 工程：PyTorch/TensorFlow、特征工程、离线训练、在线推理、A/B 实验、Hive/Spark、向量检索和分布式基础。

### 1.2 你的核心优势

| 能力 | 简历证据 | 与岗位的对应关系 |
|---|---|---|
| 因果与多目标排序 | 多档位补贴 CFR/Uplift、IPW、Rank/Pairwise、TwoStage | 广告 ROI、补贴策略、转化归因、多目标排序 |
| 行为序列基建 | Attempt 粒度、事件—Enrich—序列、亿级长历史优化 | 用户行为建模、序列建模、特征平台、Hive/Spark |
| CVR 与兴趣建模 | PCVRHyFormer、DIN、Cross-Attention、RankMixer | 广告 CVR、推荐精排、多域兴趣融合 |
| 召回与序列推荐 | itemCF + HSTU 双阶段系统 | 推荐召回、序列兴趣、负采样、Top-K 指标 |
| 数据分析和实验 | 去哪儿指标体系、规则引擎、分层分析 | A/B 实验、指标归因、策略迭代 |
| 数学统计基础 | 统计学本科、数学硕士 | 概率统计、优化、因果推断、实验设计 |

### 1.3 当前短板

| 短板 | 面试风险 | 应对方式 |
|---|---|---|
| 搜索 Query 理解经验较少 | 可能被问改写、纠错、意图、相关性 | 补全搜索链路框架，坦诚没有生产经验，用推荐召排能力迁移回答 |
| ANN/向量引擎实践不突出 | JD 明确提到 FAISS、Milvus、RecallDB | 准备双塔、HNSW、IVF、PQ、Recall@K 与延迟权衡 |
| 广告出价预算经验较少 | 可能追问 eCPM、OCPX、Pacing、ROI | 把 Uplift 与增量 ROI 联系起来，同时补预算和出价基础 |
| 在线服务描述不足 | 可能追问特征新鲜度、缓存、延迟、容灾 | 用行为序列项目回答离在线一致性，再补模型服务链路 |
| 算法基础表述过满 | 简历写“精通”容易遭遇无限追问 | 改为“熟悉/掌握”，只对能推公式、写代码的内容说精通 |

### 1.4 面试定位

最合适的候选人标签是：

> 统计与数学基础扎实，具备因果排序和深度推荐实践，能从样本、特征、模型目标到大数据计算完整落地的推荐/广告算法候选人。

不建议把自己定位成“搜索专家”或“全栈搜广推专家”。更可信的说法是：

> 我的直接经历更偏推荐、广告 CVR 和策略建模，但推荐与搜索在召回、排序、多目标和在线实验上高度共通，我正在重点补齐 Query 理解和 ANN 检索部分。

---

## 2. 自我介绍

## 2.1 60 秒版本

> 面试官您好，我叫缪可可，本科就读于中南大学统计学专业，目前在新加坡国立大学攻读数学硕士。我的经历主要围绕推荐广告中的因果排序、用户行为序列和 CVR 预估展开。
>
> 最近我在滴滴价格策略团队实习，主要做了两个项目。第一个是多档位补贴 Uplift 排序：我发现原 CFR 模型主要优化响应概率，与最终增量排序目标不完全一致，因此基于 IPW 构造排序伪标签，引入相关性 Rank Loss 和局部 Pairwise Loss，并通过 TwoStage 与软解冻控制排序梯度对因果表征的干扰，Call AUCC 相对 Baseline 提升 15% 以上，同时监控 treatment ratio 和 QINI，避免利用分流结构刷指标。第二个项目是司乘行为序列基建，我用 Attempt 统一冒泡、发单到完单链路，搭建事件、Enrich、任务序列三层架构，并用预排号、锚点截断和回表取数解决亿级样本长历史 Join 膨胀。
>
> 此外，我在推荐比赛中做过多行为域 CVR 模型，使用候选感知 DIN、HyFormer、Cross-Attention 和 RankMixer 建模多域兴趣；也做过 itemCF 加 HSTU 的召回排序系统。我的优势是能把统计因果、深度推荐和数据工程结合起来。我希望在这个岗位继续深入召回、排序、多任务和商业化目标优化，并补齐搜索意图和向量检索能力。

### 使用提醒

- “15% 以上”必须明确是相对提升还是绝对百分点；建议说“相对提升 15%+”。
- 如果该结果不是最终线上 A/B，必须说“离线 Call AUCC”，不能暗示线上收益。
- 一分钟介绍只讲两个主项目，比赛项目一句带过。

## 2.2 90～120 秒版本

> 面试官您好，我叫缪可可，本科是中南大学统计学，目前在新加坡国立大学读数学硕士。我的学习背景给了我比较扎实的概率统计、优化和机器学习基础；实践上，我主要关注推荐广告中的因果推断、用户行为建模、CVR 排序和特征工程。
>
> 我目前在滴滴价格策略团队实习。第一个项目是多档位补贴的 Uplift 排序。业务真正关心的不是用户会不会呼叫，而是某档补贴相对不补贴增加了多少呼叫。原模型用 CFR 的共享底座和多 Treatment Head 做响应预测，但训练只优化 Factual BCE，和最终 AUCC 排序目标存在错位。我基于实验分流概率构造 IPW 增量伪标签，用 Corr Rank Loss 提供全局排序监督，再用 Matched Pairwise 补局部顺序；为了防止高方差排序梯度破坏响应表征，我设计两阶段训练，第一阶段学稳定的响应概率，第二阶段冻结 Embedding，并用 1% 学习率软解冻共享底座、重点训练 Head。Rank Loss 内又使用分档位 Z-score 和温度为 2 的 Tanh 压制长尾极值。最终离线 Call AUCC 相对提升 15%+，并通过 treatment-ratio 和 QINI 检查因果排序的可信度。
>
> 第二个项目是司乘行为序列建设。原来大量使用均值、次数等统计特征，会丢失行为顺序和看价未发单反馈，而且冒泡、订单和履约链路分散。我以 Attempt 为统一业务根，设计事件、Attempt/Order Enrich、任务序列三层架构。价格侧把观测窗口前置到冒泡阶段，保留不同价格锚点和供需下的决策；工程上把历史先做成 ID、时间戳、序号、事件键的窄索引，先按样本锚点定位边界并截到 128 条，再回表取宽特征，降低亿级样本长历史构造的中间结果和 Shuffle。
>
> 在校项目方面，我做过 PCVRHyFormer 的多域行为序列 CVR 优化，把静态特征 Token 化，对四路行为序列加入时间桶，并在 Query 生成阶段加入候选感知 DIN，随后用 HyFormer、Cross-Attention 和 RankMixer 做域内提取和跨域融合；还做过 itemCF 与 HSTU 的双阶段推荐。因此我与岗位中用户行为、序列建模、CTR/CVR、多任务和数据落地匹配度较高。搜索侧的 Query 理解和 ANN 是我希望进一步加强的方向。

## 2.3 HR 或主管面版本

> 我是统计学本科和数学硕士背景，职业方向一直比较明确，希望做推荐、广告和策略算法。我的特点是不会只从模型结构出发，而是习惯先确认业务目标、样本时点和指标是否一致，再做模型和工程优化。
>
> 在滴滴，我既做过多档位补贴的因果排序模型，也做过司乘行为序列数据底座；前者让我积累了从业务目标、Loss 设计到离线评估的经验，后者让我补齐了大规模特征工程和数据口径。比赛项目又让我系统实践了 CVR、序列兴趣和召回排序。我希望进入覆盖搜索、推荐、广告的团队，接触更完整的召排链路和线上系统。

## 2.4 “为什么选择这个岗位”

> 这个岗位同时覆盖搜索、推荐、广告和工程落地，与我现有经历的交集很大。我的多档位 Uplift 项目对应商业化中的转化增量和 ROI，多域 CVR 与行为序列对应推荐和广告精排，itemCF/HSTU 对应召回与序列推荐，Hive/Spark 序列基建对应特征工程。另一方面，岗位中的 Query 理解、向量召回和在线检索又是我下一阶段希望补齐的能力，所以它既匹配已有积累，也有明确成长空间。

---

## 3. 项目总梳理

## 3.1 项目优先级

面试中建议按岗位方向选择项目顺序：

| 面试方向 | 第一项目 | 第二项目 | 第三项目 |
|---|---|---|---|
| 商业广告/策略 | 多档位 Uplift | PCVRHyFormer | 司乘序列 |
| 推荐排序 | PCVRHyFormer | 司乘序列 | itemCF + HSTU |
| 推荐召回 | itemCF + HSTU | PCVRHyFormer | 司乘序列 |
| 数据/工程落地 | 司乘序列 | 多档位 Uplift | 去哪儿指标平台 |
| 综合一搜广推 | 多档位 Uplift | PCVRHyFormer/司乘序列 | itemCF + HSTU |

## 3.2 五个项目一张表

| 项目 | 问题 | 核心动作 | 结果 | 岗位映射 |
|---|---|---|---|---|
| 多档位补贴 Uplift | CFR 响应目标与 AUCC 排序错位 | IPW 伪标签、Corr Rank、Matched Pairwise、Z-score+Tanh、TwoStage Soft-Unfreeze | 离线 Call AUCC 相对提升 15%+；结构稳定性约束 | 广告 ROI、因果归因、多目标排序 |
| 司乘行为序列 | 统计特征损失、冒泡到履约割裂、历史 Join 膨胀 | Attempt 粒度、三层架构、Bubble 未发单、窄索引边界截断回表 | 材料缺少量化结果，需补 | 行为建模、Hive/Spark、特征平台 |
| PCVRHyFormer | 多域序列异构、静态语义错位、候选感知不足 | 8 个静态 Token、int+dense 融合、时间桶、前置 DIN、HyFormer、RankMixer | 已记录 AUC 约 0.8255～0.826，比赛 Top 15% | 广告 CVR、精排、兴趣建模 |
| itemCF + HSTU | 召回相关性/多样性与长序列排序 | itemCF 校正、类目降权、HSTU、InfoNCE、混合负采样 | HR@10 16.92%、NDCG@10 0.1168、多样性 0.73 | 推荐召回、序列排序、负采样 |
| 去哪儿数据分析 | 复杂规则执行偏差、指标定位困难 | 规则引擎、看板、10+ 维下钻、千万级日志 | 效率 +20%+，差错率 -16% | 指标体系、实验分析、策略迭代 |

---

## 4. 四个技术项目的标准讲法

## 4.1 多档位补贴因果排序

### 一句话

> 把只优化多档位响应概率的 CFR Baseline，升级为兼顾 Factual Probability 和 Uplift Ranking 的两阶段因果排序模型。

### STAR 结构

**Situation：** 多档位补贴希望把预算给真正会被激励的用户，而不是本来就会呼叫的人。

**Task：** 原 CFR 使用共享表示和多档位 Head，只在 Observed Arm 上优化 Factual BCE；线上/离线关心的却是各档相对 Control 的 Uplift 排序和 Call AUCC。

**Action：**

1. 用实验分流概率/IPW 和 $2y-1$ 构造高方差但方向有效的增量伪标签；
2. 用 Corr Rank Loss 做全局稠密排序监督；
3. 用匹配后的 Pairwise Loss 补充局部 treatment-control 顺序；
4. 分档对 Uplift Score 做 Z-score，再用 $\tanh(z/T)$、$T=2$ 压缩极值；
5. TwoStage：先 Factual Anchor，后冻结 Embedding、共享底座 1% 学习率软解冻、主训 Heads；
6. 除 AUCC 外监控 QINI、Treatment Ratio、分档稳定性，防止模型利用 treatment identity 或历史分配捷径。

**Result：** 离线 Call AUCC 相对 Baseline 提升 15%+；最终方案选择不是只看最高分，而是在 Treatment Ratio 相对稳定的候选中选择 AUCC 最优点。

### 必须会回答

1. 为什么响应预测不等于 Uplift？
2. CFR Baseline 的结构和 Loss 是什么？
3. IPW 伪标签为什么在期望上有效、单样本却高方差？
4. 为什么用 $2y-1$，四格样本各代表什么？
5. Corr Loss 为什么适合排序，为什么不用 MSE？
6. Pairwise Pair 如何匹配，为什么不能全局随机配对？
7. 为什么 Rank Loss 会污染底层表示？
8. TwoStage、Freeze 与 Soft-Unfreeze 的区别？
9. Z-score 为什么放在每个 treatment 内？
10. Tanh 为什么带温度 $T=2$？
11. AUCC/QINI 怎么计算，真实 ITE 不可见如何评估？
12. Treatment Ratio 不平说明什么？
13. RCT 下为什么还需要 IPW？
14. 15% 是绝对还是相对、统计显著吗？
15. 如果上线，如何把 Uplift 与补贴成本结合？

### 最容易被质疑的点

> 不能把 IPW 伪标签说成用户真实 ITE；不能把离线 AUCC 提升说成线上 ROI；不能只用 AUCC 证明因果可信，需要同时说明随机化粒度、Overlap、Treatment Ratio 和置信区间。

## 4.2 司乘行为序列建设

### 一句话

> 用 Attempt 统一一次出行决策链路，把统计特征升级成防穿越的司乘长序列，并通过窄索引预排号和锚点截断解决亿级样本历史回查膨胀。

### STAR 结构

**Situation：** 价格与交易模型依赖近 7/30 天统计值，行为顺序和上下文被压缩；Bubble、Order、履约链路口径割裂。

**Task：** 建设能同时服务价格和交易任务的序列底座，纳入未发单反馈，又不能生成万能宽表或造成未来信息穿越。

**Action：**

1. 以 Attempt/去重 Bubble Trace 表示一次完整出行意图；
2. 事件层统一 Bubble、Call、Answer、Cancel、Finish 等；
3. Attempt Enrich 承接发单前价格与供需，Order Enrich 承接发单后履约；
4. 序列层围绕任务样本和 Anchor Time 按需生成；
5. 价格侧把历史前置到 Bubble，纳入看价未发单；
6. 历史先生成 `ID + event_time + sequence_no + event_key` 窄索引；
7. 对每个 Anchor 定位 120 天窗口和最近 128 条边界，再回宽表取数；
8. 用事件时间、可用时间、稳定排序键和 Mask 保证离在线一致。

**Result：** 当前简历没有量化计算收益或模型收益，面试前必须补充任务耗时、Shuffle、覆盖率或模型消融数据。

### 必须会回答

1. 为什么是 Attempt 而不是 Order？
2. Bubble 未发单为什么重要，是否一定表示价格拒绝？
3. Event、Attempt Enrich、Order Enrich、Sequence 各自粒度？
4. 为什么不合并成万能宽表？
5. 改派、多 Bubble、取消阶段如何归因？
6. `dt < sample_dt` 为什么不能完全防穿越？
7. Event Time 与 Available Time 有什么区别？
8. 朴素 Range Join 为什么会膨胀？
9. 预排号为什么有效，Sequence No 如何稳定？
10. `COLLECT_LIST` 是否保证顺序？
11. 高频 ID 数据倾斜如何处理？
12. 为什么 120 天、128 条？
13. 序列如何输入 DIN/SASRec/HSTU？
14. 如何验证优化前后序列等价？
15. 这个项目不是 ETL 的算法价值是什么？

## 4.3 PCVRHyFormer 多域序列 CVR

### 一句话

> 面向四路异构行为序列，把静态上下文、候选物品和历史兴趣统一成 Token，并在 Query 生成阶段做候选感知软匹配，再用 HyFormer 与 RankMixer 完成域内提取和跨域融合。

### STAR 结构

**Situation：** 多行为域字段不同、序列长度不同；同 FID 的类别 ID 和统计量语义错位；历史兴趣提取缺少当前候选感知。

**Task：** 在控制模型容量和泛化风险的前提下，提高广告 CVR 排序能力。

**Action：**

1. 用户、Item、请求时间等静态信息压缩为少量 NS Tokens；
2. 同 FID 的类别 Embedding 与 Dense Statistic Projection 做 Element-wise 对齐融合；
3. 四路序列分别编码，并加入时间间隔 Bucket；
4. MultiSeqQueryGenerator 为每个域生成多个兴趣 Query；
5. 最终 516 版本把候选 Item 的 Target-aware DIN Pooling 放在 Query 生成阶段；
6. Query 通过 Cross-Attention 读取对应序列；
7. RankMixer 用轻量 Token Mixing 融合各域 Query 与静态 Token；
8. 使用 Adagrad/AdamW 分组优化、梯度裁剪；通过消融淘汰硬匹配、盲目加长序列和过大模型。

**Result：** 本地记录约 0.8255～0.826，比赛 Top 15%。简历更稳妥写“测试 AUC 约 0.83”或“0.826”，不建议写“0.83+”。

### 必须会回答

1. PCVR 是什么，与 CTR/CVR 有什么关系？
2. Baseline 是什么？真正有效的改动是哪几个？
3. 为什么将静态特征 Token 化，而不是全部 Concat？
4. 为什么同 FID 的 ID 和统计量逐元素加法？
5. 四路序列为什么分域编码？
6. Query Token 如何生成？
7. 516 前置 DIN 与后置 DIN Residual 有什么区别？
8. 为什么候选感知应在 Query 生成阶段进入？
9. HyFormer 的 Sequence Evolution、Query Decoding、Query Boosting 是什么？
10. RankMixer 与直接堆 MLP/Self-Attention 的区别？
11. 为什么更长序列、更大 $d_{model}$ 反而下降？
12. 硬匹配特征为什么从 0.826 降到约 0.820？
13. Adagrad 和 AdamW 为什么分组使用？
14. AUC 与 LogLoss 的区别，比赛为什么看 AUC？
15. 如何确认不是高基数 ID 记忆？

### 关键口径

> 最终代码口径是“516 前置 Candidate-aware Query Pooling”，不要再把后置 DIN Residual 说成最终最优；也不要把所有实验模块都包装成最终模型贡献。

## 4.4 itemCF + HSTU 双阶段推荐

### 一句话

> 用带时间、活跃度、热度和类目多样性校正的 itemCF 做召回，再用 HSTU 类序列编码器与对比学习完成候选排序。

### STAR 结构

**Situation：** 需要同时保证候选覆盖、近期兴趣、长尾和类目多样性，并利用用户行为序列精排。

**Task：** 搭建可解释的双阶段推荐：召回阶段保证覆盖和效率，排序阶段学习长序列兴趣。

**Action：**

1. itemCF 计算物品共现并加入时间衰减；
2. 对高活跃用户贡献做归一/惩罚，避免重度用户主导；
3. 加全局热度平滑和近期类目偏好；
4. 候选类目过度拥挤时降权，提高多样性；
5. 排序使用 HSTU 类 Pointwise Attention、门控与时序 Bias；
6. InfoNCE 使用 L2 Normalization、Temperature 0.07；
7. 负样本按 6:4 混合随机与较难负样本。

**Result：** HR@10 16.92%、NDCG@10 0.1168、类目多样性 0.73。必须同时说明数据切分、候选规模和负样本协议，否则指标无法横向比较。

### 必须会回答

1. itemCF 相似度公式，热门物品为什么会污染共现？
2. 时间衰减、用户活跃度惩罚和热度校正分别解决什么？
3. 为什么召回与排序分开？
4. HR@K、Recall@K、NDCG@K 的区别？
5. 类目多样性如何定义，和准确率如何权衡？
6. HSTU 的 Pointwise Attention 与 Softmax Attention 区别？
7. HSTU Dense Attention 的复杂度是否仍为 $O(L^2d)$？
8. HSTU 相比 Transformer 主要节省在哪里？
9. InfoNCE 的公式和温度作用？
10. 为什么要 L2 归一化？
11. 6:4 混合负样本是哪两类，比例怎么验证？
12. False Negative 怎么处理？
13. 召回离线指标提高为什么不一定带来最终 NDCG？
14. 如果上线上量，如何做 ANN、缓存和增量更新？

### HSTU 表述修正

不建议继续说：

> “HSTU 用空间门控向量和 Pointwise SiLU 替换 FFN 与 Softmax，所以把复杂度降成线性。”

更准确的回答是：

> HSTU 的 Dense Attention 仍包含 $O(L^2d)$ 项；它通过 Pointwise Attention、相对时间/位置 Bias、门控 Transformation、更少的 Attention 外线性层、Jagged/Fused Kernel、Stochastic Length 和历史计算复用，更贴合推荐行为流并降低实际计算与内存开销。

---

## 5. 岗位映射式回答

## 5.1 如果问“你更适合搜索、推荐还是广告？”

> 我的直接项目更偏推荐和广告：Uplift 对应商业化增量与 ROI，PCVRHyFormer 对应广告 CVR 和精排，司乘序列与 HSTU 对应行为/序列推荐。搜索侧我具备召回排序的通用基础，但 Query 理解、纠错改写和 ANN 生产系统经验相对少。我希望以推荐广告能力为切入点，逐步扩展到完整搜索链路。

## 5.2 如果问“你没有搜索经验，为什么投一搜广推？”

> 搜索与推荐的输入不同，但核心链路有明显共性：多路召回、粗排精排、多目标重排、负采样、相关性/转化预估和在线实验。我在 itemCF 项目做过召回，在 PCVR 做过精排和候选感知兴趣，在滴滴做过多目标因果排序。我的缺口主要是 Query 侧和索引工程，这部分边界明确，也正是我在补的内容，而不是从零开始理解召排系统。

## 5.3 如果问“你对商业广告的理解是什么？”

> 商业广告不是单纯把 CTR 做高，而是在用户体验、广告主 ROI 和平台收入之间做约束优化。预测层需要 CTR/CVR/Value，策略层还要考虑出价、预算 Pacing、流量分配、归因和反作弊。我的 Uplift 项目强调增量而不是自然转化，能够减少对本来就会响应用户的无效补贴，这与增量 ROI 的思路一致。

## 5.4 如果问“你能做工程落地吗？”

> 我的司乘序列项目不是只训练模型，而是从事件主键、预测时点、Hive/Spark 范围 Join、数据倾斜、序列顺序到离在线一致性完整设计。我会先定义数据契约和防穿越口径，再优化计算，并通过新旧链路逐样本 Diff 验证语义一致。模型工程方面使用过 PyTorch、Embedding、Attention、分组优化和梯度裁剪，但大规模在线 Serving 与分布式训练是我还需要继续加强的部分。

---

## 6. 高频综合面试问题

## 6.1 开场与项目选择

### Q1：你最有代表性的项目是什么？

> 如果面试偏广告策略，我会选多档位 Uplift，因为它完整体现了从业务目标错位、Loss 设计、优化冲突到评估可信度的过程；如果偏推荐模型，我会选 PCVRHyFormer；如果偏工程，我会选司乘行为序列。

### Q2：哪个项目最难？

> Uplift 项目的难点是没有真实 ITE 标签，却要优化排序并防止模型利用分流捷径；序列项目的难点是业务粒度、预测时点与大规模计算必须同时正确。我会根据面试方向选择一个展开，不把困难只描述成调参。

### Q3：你做失败过什么？

> PCVR 项目中，显式 Item-History 硬匹配把 AUC 从约 0.826 降到 0.820，更长序列和更大模型也没有提升。复盘发现强匹配容易记忆高基数 ID 和短期分布，增加长度也会引入陈旧噪声。因此最终保留软 Attention、较小维度和前置候选感知 Query，而不是继续堆模块。

## 6.2 基础模型

### Q4：LR、FM、DeepFM 的演进关系？

> LR 是线性加权，交叉依赖人工构造；FM 用低秩 Embedding 内积自动学习稀疏二阶交叉；DeepFM 保留 FM 的显式一二阶，同时用 DNN 学隐式高阶交互，并共享 Embedding。

### Q5：GBDT 为什么适合表格数据？

> 树路径天然学习阈值和高阶条件组合，不依赖归一化，对非线性和缺失模式较强。Boosting 中新树通用地拟合损失的负梯度，只有平方误差下最直观地等于残差。

### Q6：DIN 与 SASRec 的区别？

> DIN 用候选作为 Query，从历史中挑当前相关兴趣，原始模型不强调顺序；SASRec 用 Causal Self-Attention 和位置编码学习行为演化，得到候选无关的序列状态。DIN 单候选约 $O(Ld)$，SASRec Dense Attention 单层约 $O(L^2d+Ld^2)$。

### Q7：MMoE 与 PLE 的区别？

> MMoE 共享所有 Experts，每个任务用独立 Gate 选择组合；PLE 同时设置共享和任务私有 Experts，并逐层抽取，更能隔离任务冲突，但参数和优化更复杂。

## 6.3 召回与排序

### Q8：推荐系统为什么分召回、粗排、精排、重排？

> 候选库规模巨大，不可能对全部 Item 使用重模型。召回保证覆盖并将百万级缩到千级，粗排进一步低成本筛选，精排用复杂特征和模型预测价值，重排处理多样性、公平、业务规则和全局约束。

### Q9：多路召回如何融合？

> 先保证各路分数可比或使用路内 Rank，再做去重、配额、加权和学习式融合；同时监控每路覆盖率、命中率、独占贡献和后续精排收益。不能只按召回数量平均分配，因为不同通道质量和人群覆盖不同。

### Q10：双塔召回如何训练？

> User Tower 和 Item Tower 产生同维向量，用内积或 Cosine 打分；通过 Sampled Softmax/InfoNCE 让正样本靠近、负样本远离。训练关键是负采样、In-batch 偏差、温度、向量归一化和 Item 频率校正，服务时将 Item 向量建 ANN 索引。

### Q11：粗排为什么不能直接复用精排？

> 粗排候选更多、延迟预算更小，很多实时交叉特征不可用；需要轻量模型、特征蒸馏或双塔/浅层网络。直接复用精排会导致计算不可承受，而且训练样本分布也不同。

### Q12：重排主要做什么？

> 在相关性/价值分基础上处理类目和作者多样性、去重、公平、广告密度、预算、频控和列表级交互。常见方法包括规则、MMR、DPP、列表模型、约束优化和 RL。

## 6.4 指标与实验

### Q13：AUC、GAUC、LogLoss 有什么区别？

> AUC 衡量全局正样本排在负样本前的概率；GAUC 先按用户/请求等分组算 AUC 再加权，更贴合组内排序；LogLoss 衡量概率似然并对高置信错误惩罚强。AUC 好不代表校准好，GAUC 分组内必须同时有正负样本。

### Q14：Recall@K、HR@K、NDCG@K？

> Recall@K 衡量 Top-K 找回真实相关集合的比例；单正样本场景中 HR@K 表示是否命中；NDCG@K 进一步按排名位置折损，更关注把相关物品放在前面。

### Q15：离线提升为什么可能线上无收益？

> 可能是样本选择、负采样、位置偏差、特征穿越、离在线不一致、校准恶化、目标与业务价值错位或收益集中在非关键人群。需要分桶、分场景和在线 A/B 验证，不能只看总体 AUC。

### Q16：A/B 实验如何设计？

> 明确随机化单位、主指标、护栏指标、MDE、显著性和实验周期；检查 SRM、AA 一致性和网络效应。出行或广告场景可能有双边干扰，用户级随机并不一定满足 SUTVA，需要考虑城市/时段或 Switchback 设计。

## 6.5 工程与数据

### Q17：Embedding 为什么常成为推荐模型瓶颈？

> 高基数 Embedding 参数量大，但单次只查少量行，FLOPs 不高却受内存带宽、参数服务器通信和热点 Key 影响。需要考虑分片、缓存、频率裁剪、Mixed Dimension 和稀疏优化器。

### Q18：Adagrad 为什么常用于稀疏 Embedding？

> 它按参数累积平方梯度，为不同出现频率的 ID 提供自适应步长，低频特征仍能获得较大更新。缺点是累积量单调增大，后期学习率可能过小且需要额外状态内存。

### Q19：如何处理 Hive/Spark 数据倾斜？

> 先定位热点 Key 和 Join 基数，再按情况使用预聚合、过滤、广播小表、热点拆分加盐、分桶排序、AQE Skew Join；根本上要减少无界 Join 和重复数据。只加 Executor 通常不能解决单个热点 Task。

### Q20：如何保证离在线一致？

> 统一特征定义、时间语义、词表、归一化、默认值、序列排序与截断；做样本回放和逐字段 Diff；区分 Event Time 与 Feature Available Time；监控线上缺失率和分布漂移。

---

## 7. 面试前技术复习优先级

## 7.1 P0：简历上的内容，必须能深入两到三层

### 因果推断与 Uplift

- Potential Outcome、ATE/CATE/ITE；
- SUTVA、Ignorability、Overlap、Consistency；
- Propensity Score、IPW、Clipping、Effective Sample Size；
- CFR、DR-CFR、EFIN 的目标与差异；
- AUUC、QINI、AUCC；
- Transformed Outcome、Pairwise Ranking；
- RCT 随机化单位、Treatment Ratio、置信区间；
- Uplift 与 Cost/ROI 的策略结合。

### 序列模型

- DIN 公式、Mask、是否 Softmax、候选相关计算；
- MHA 手写、Causal Mask、Padding Mask；
- SASRec 的目标、负采样和复杂度；
- HyFormer 的 Query Token、Cross-Attention、RankMixer；
- HSTU Pointwise Attention、时间 Bias、门控与复杂度；
- 长序列截断、时间桶、位置编码、False Negative。

### 多任务与多目标

- Shared Bottom、MMoE、PLE、ESMM、AITM；
- Expert/Gate 公式、死专家；
- 梯度 Cosine、PCGrad、GradNorm；
- Loss Weight 与 Serving Weight 区别；
- 加权目标、Lagrangian、Pareto。

### 工程

- PyTorch Forward/Backward、Optimizer、Gradient Clipping；
- Adagrad、AdamW、Weight Decay；
- Hive 分区、Join、Window Function、Collect List 排序；
- Spark Shuffle、Skew、Broadcast、AQE；
- 数据穿越、迟到回刷、离在线一致性；
- A/B 实验与指标监控。

## 7.2 P1：JD 明确要求，但简历证据较少

### 搜索链路

1. Query Intent：导航型、信息型、交易型、类目/属性意图；
2. Query Rewrite：同义改写、实体标准化、扩展与约束；
3. Spell Correction：候选生成 + 语言/相关性排序；
4. 类目匹配：Query-Category 多分类或语义匹配；
5. 相关性：BM25、双塔、Cross-Encoder、Learning to Rank；
6. 倒排索引：Term Dictionary、Posting List、BM25；
7. 搜索指标：NDCG、MRR、Recall、点击/转化/满意度。

### 向量检索

- Exact KNN 与 ANN；
- HNSW：图搜索，Recall 高、内存大；
- IVF：先聚类选倒排桶，再桶内搜索；
- PQ：向量分段量化，压缩内存但损失精度；
- FAISS IndexFlat、IVF-PQ、HNSW 的取舍；
- Recall@K、QPS、P95/P99、Index Size、Build Time；
- 增量更新、删除、一致性和冷热分层。

### 商业广告

- CTR、CVR、CTCVR 与 ESMM；
- CPM/CPC/CPA、eCPM；
- ROI 与 Incremental ROI；
- Bidding/OCPX 基本逻辑；
- Budget Pacing；
- Last-click、Multi-touch、因果归因；
- 频控、反作弊、用户体验护栏。

## 7.3 P2：加分项

- 冷启动：内容/属性、Meta Learning、Exploration、Bandit；
- 多样性：MMR、DPP、Coverage、Intra-list Diversity；
- 公平性：Exposure Fairness、Group Constraints；
- LLM 搜索：Query 改写、Embedding Recall、Reranker、RAG；
- 强化学习：Slate/长期价值、Off-policy Evaluation；
- Kafka、Redis、Feature Store、Model Serving 基础。

---

## 8. 搜索与 ANN 短板的安全回答

## 8.1 “你用过 FAISS/Milvus 吗？”

如果没有真实生产经验，不要假装：

> 我目前没有把 FAISS/Milvus 用在生产服务中的经验。项目中做过 itemCF 候选生成和向量对比学习，对召回的训练与指标比较熟悉；我理解 ANN 服务需要在 Recall、QPS、P99 和内存间权衡，也系统学习了 HNSW、IVF-PQ 等索引。若进入岗位，我可以从离线双塔与 FAISS 验证开始，再补增量索引和在线服务。

## 8.2 “你没做过 Query 改写，怎么做？”

> 我会先定义改写目标和风险：纠错、同义扩展、实体标准化、属性补全不能改变原意。候选可来自规则/词典、日志共现、生成模型；排序使用 Query-Query 或 Query-Doc 语义、点击转化反馈和安全约束。评估既看 Recall/NDCG，也要看 Bad Rewrite Rate 和无结果率。我的生产经验不在 Query 侧，但可以把候选生成—排序—离线评估—A/B 的方法迁移过来。

---

## 9. 简历表述风险与修改建议

## 9.1 PCVR AUC

当前写法：

> 最终测试集 AUC 达到 0.83+

问题：已有项目笔记记录的最佳结果约为 0.8255～0.826，“0.83+”容易被理解为大于 0.83。

建议：

> 最终测试集 AUC 约 0.83（0.826），进入比赛 Top 15%。

或只写精确值：

> 最终测试集 AUC 达到 0.826，进入比赛 Top 15%。

## 9.2 HSTU 描述

当前写法：

> 复现 HSTU 以空间门控向量和 Pointwise SiLU 替换传统 FFN 与 Softmax，降低长行为序列建模开销。

建议改为：

> 复现 HSTU 类推荐序列编码器，引入 Pointwise Attention、相对时间/位置偏置与门控 Transformation，并结合变长序列计算降低实际训练开销。

如果没有实现论文完整 Jagged Kernel，不要暗示复现了全部工业优化。

## 9.3 多任务与多目标

当前写法：

> 熟练应用 MoE、MMoE、PLE 等多目标联合框架。

问题：MMoE/PLE 首先是多任务参数共享结构，不等同于多目标决策。

建议：

> 熟悉 Shared Bottom、MMoE、PLE 等多任务学习架构，理解梯度冲突、样本空间差异与多业务目标权衡。

## 9.4 “精通机器学习、深度学习基础理论”

建议改为：

> 掌握机器学习与深度学习基础，熟悉 LR、GBDT、FM、DeepFM、DIN、Transformer 等模型的原理与应用。

“精通”会让面试官从任意算法证明、源码和工程细节无限追问。

## 9.5 Uplift 收益

当前写法：

> 提升 Call AUCC（15%+）

建议明确：

> 离线 Call AUCC 相对 CFR Baseline 提升 15%+，并保持各分档 Score Bin 的 Treatment Ratio 基本稳定。

如果有多次实验均值、置信区间或绝对值，最好补上。

## 9.6 AI 提效

AI 工具可以写，但面试官可能质疑代码原创性。推荐回答：

> 我用 AI 做代码骨架、单测、文档和资料检索，但模型目标、实验设计、数据口径和结果判断由我负责；生成代码会经过源码阅读、Case 验证和实验复现，不直接把输出作为结论。

---

## 10. 行为面试准备

## 10.1 为什么从统计学转算法？

> 统计学让我关注估计、偏差和不确定性，但我更希望把这些方法用于大规模真实决策。推荐广告正好需要概率预测、因果实验、优化和工程落地，所以我在研究生和实习阶段持续补深度学习、序列建模和大数据工程。

## 10.2 你如何推动项目？

> 先对齐业务指标和样本口径，再建立可复现 Baseline；对模型改动做控制变量实验；对关键结论保留失败 Case 和监控；最后将有效模块沉淀成可复用数据或训练组件。司乘序列项目体现的是跨链路口径推动，Uplift 项目体现的是目标函数与评估闭环。

## 10.3 与他人意见不一致怎么办？

> 把争议转成可验证假设。例如“更长序列一定更好”可以通过长度覆盖率、不同长度消融和训练成本验证；“最高 AUCC 就最好”可以加入 Treatment Ratio、QINI 和稳定性约束比较。先统一评价标准，再讨论方案，而不是争论模型偏好。

## 10.4 你最大的不足是什么？

> 我的项目更偏推荐广告与离线大数据建模，搜索 Query 理解、ANN 线上服务和超大规模分布式训练经验还不够完整。我已经把缺口拆成可执行内容：补搜索链路、实现双塔+FAISS 基线、掌握 HNSW/IVF-PQ 权衡，并在现有序列项目中加强离在线 Serving 思维。

---

## 11. 面试当天回答原则

1. 先说结论，再说机制，不要先铺五分钟背景。
2. 项目回答固定使用：业务目标 → Baseline → 痛点 → 改动 → 原因 → 结果 → 局限。
3. 所有提升说明：离线/线上、绝对/相对、哪个 Baseline、哪个数据集。
4. 真实 ITE 不可观测；Attention Weight 不等于因果解释；AUC 不等于校准。
5. 不会的生产系统坦诚边界，再给出结构化解决方案。
6. 不把合理想法说成已验证贡献，不把实验分支说成最终代码。
7. 每个复杂模型都准备一个“为什么不用更简单模型”的回答。
8. 主动提失败实验，会比堆砌模块更体现研究能力。
9. 被问工程时讲数据规模、瓶颈、复杂度、监控和一致性，不只说用了 Spark。
10. 最后反问团队：主要方向、召排链路、线上指标、模型迭代方式和新人职责边界。

---

## 12. 最后速记

### 你的三条能力主线

```text
统计与因果：Uplift / IPW / CFR / A-B Test
推荐与序列：DIN / HyFormer / HSTU / itemCF / CVR
数据与工程：Attempt / Hive-Spark / 长历史 / 离在线一致性
```

### 最重要的三个项目结论

1. Uplift：把响应预测升级为增量排序，但通过 TwoStage 和结构诊断防止排序梯度走捷径。
2. 司乘序列：用 Attempt 统一业务语义，用预排号和有界回表解决长历史计算。
3. PCVR：候选感知应进入 Query 生成阶段，软交互优于硬匹配，复杂度增加不等于泛化提升。

### 岗位匹配一句话

> 我已经具备推荐广告中的 CVR、因果排序、行为序列和大数据特征工程经验，可以从推荐/广告方向快速贡献；同时希望在团队中补齐搜索 Query 理解、向量检索和线上服务能力。
