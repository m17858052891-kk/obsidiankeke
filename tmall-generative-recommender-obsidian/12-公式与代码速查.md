# 12 公式与代码速查

## 1. 共现权重

$$
w_{ij} \mathrel{+}= b(a_i)b(a_j)
\exp(-|t_i-t_j|/\tau_t)
\frac{1}{\log(1+L_u)}
\gamma^{d(i,j)-1}
$$

## 2. Item2Vec score

$$
s(i,j)=\hat E_i^T\hat E_j,
\quad \hat E_i=E_i/\|E_i\|_2
$$

$$
\mathcal{L}_{pair}=-w_{ij}\log\sigma(s(i,j))
-\sum_{n\in N(i)}\log\sigma(-s(i,n))
$$

## 3. 残差量化

$$
r_0=z,\quad k_l=\arg\min_k\|r_l-C_l[k]\|^2
$$

$$
q_l=C_l[k_l],\quad r_{l+1}=r_l-q_l,\quad q=\sum_l q_l
$$

## 4. RQ-VAE 风格损失

$$
\mathcal{L}_{RQ}=\|D(q(z))-z\|^2
+\lambda_c\|\operatorname{sg}[z]-q(z)\|^2
+\beta\|z-\operatorname{sg}[q(z)]\|^2
$$

## 5. SID 概率分解

$$
p(s_{0:L-1}|h)=\prod_{l=0}^{L-1}p(s_l|h,s_{<l})
$$

## 6. HSTU-style 核心

$$
[Q,K,V,U]=XW_{qkvu}
$$

$$
A=\operatorname{SiLU}(QK^T+B),\quad H=AV
$$

$$
Y=X+\operatorname{Dropout}((H\odot U)W_o)
$$

## 7. 指标

$$
HR@K=\frac{1}{M}\sum_m\mathbf{1}(y_m\in R_K(m))
$$

$$
NDCG@K=\frac{1}{M}\sum_m\frac{\mathbf{1}(y_m\in R_K(m))}{\log_2(rank_m+1)}
$$

$$
Coverage@K=\frac{|\cup_mR_K(m)|}{|I_{eval}|}
$$

## 8. 关键张量形状

```text
history_sid_codes : [B, T, L_sid]
history_embedding  : [B, T, D]
HSTU hidden        : [B, T, D]
target_sid_codes   : [B, L_sid]
code logits level  : [B, K_l]
beam prefix        : [beam_size, current_level]
```

## 9. 伪代码

```python
# 1. collaborative representation
pairs = weighted_cooccurrence(user_sequences)
item_vec = train_item2vec(pairs, negative_sampling=True)

# 2. discrete item address
sid_codes = rqvae.fit_transform(item_vec)
sid_table = build_sid_table(item_ids, sid_codes, collision=True)
prefix_tree = build_prefix_index(sid_table)

# 3. generative recommendation
history_repr = hstu(history_sid, behavior, time_gap)
loss = teacher_forced_sid_loss(history_repr, target_sid)
pred_sid = constrained_beam_search(history_repr, prefix_tree)
items = map_sid_to_items(pred_sid)
```

