# LeetCode Hot 100 — 技巧

---

## 136. 只出现一次的数字

**题目描述：** 给定非空整数数组，除一个元素外其余每个元素均出现两次，找出只出现一次的元素（线性时间，常数空间）。

**示例：** 输入：`nums = [4,1,2,1,2]`；输出：`4`。

**思路：** 异或。相同数字异或为 0，0 与任何数异或等于自身。对所有数做异或，最终结果就是只出现一次的数。

**展开理解：** 异或满足交换律、结合律，且 x^x=0、x^0=x。所有成对元素无论顺序如何最终都相互抵消，只剩出现一次的元素；该方法依赖其他元素恰好出现两次的题设。

```python
def singleNumber(nums):
    res = 0
    for num in nums:
        res ^= num
    return res
```

**复杂度：**
- 时间：O(n)
- 空间：O(1)

---

## 169. 多数元素

**题目描述：** 找出数组中出现次数超过 ⌊n/2⌋ 的元素（保证存在）。

**示例：** 输入：`nums = [2,2,1,1,1,2,2]`；输出：`2`。

**思路（Boyer-Moore 投票算法）：** 候选人 + 计数。遇到与候选人相同的数 +1，不同 -1，计数归零时换候选人。最终候选人即多数元素。

**展开理解：** 把多数元素与其他元素两两抵消，多数元素因出现次数超过一半，最终不可能被全部抵消。count 为 0 时此前前缀已完全配对，可从当前元素重新选择候选；若题目不保证存在多数，还需二次验证。

```python
def majorityElement(nums):
    candidate = count = 0
    for num in nums:
        if count == 0:
            candidate = num
        count += 1 if num == candidate else -1
    return candidate
```

**复杂度：**
- 时间：O(n)
- 空间：O(1)

---

## 75. 颜色分类

**题目描述：** 给定包含 0、1、2 的数组，原地将其排序，使 0 在前、1 居中、2 在后（荷兰国旗问题）。

**示例：** 输入：`nums = [2,0,2,1,1,0]`；输出：`[0,0,1,1,2,2]`。

**思路：** 三指针：`lo` 指向 0 的边界，`hi` 指向 2 的边界，`cur` 是当前遍历指针。
- `nums[cur] == 0`：与 lo 交换，lo++，cur++
- `nums[cur] == 2`：与 hi 交换，hi--（cur 不动，因为换来的数未处理）
- `nums[cur] == 1`：cur++

**展开理解：** 维护三个区域：[0,lo) 全是 0，[lo,cur) 全是 1，(hi,n-1] 全是 2。cur 遇 2 与 hi 交换后不能前进，因为换回来的元素尚未分类；遇 0 时换回的是已处理区边界的 1，可同时前进。

```python
def sortColors(nums):
    lo, cur, hi = 0, 0, len(nums) - 1
    while cur <= hi:
        if nums[cur] == 0:
            nums[lo], nums[cur] = nums[cur], nums[lo]
            lo += 1
            cur += 1
        elif nums[cur] == 2:
            nums[hi], nums[cur] = nums[cur], nums[hi]
            hi -= 1
        else:
            cur += 1
```

**复杂度：**
- 时间：O(n)
- 空间：O(1)

---

## 31. 下一个排列

**题目描述：** 给定整数数组，将其重排成字典序上的下一个排列，若已是最大排列则重排为最小排列（原地）。

**示例：** 输入：`nums = [1,2,3]`；输出：`[1,3,2]`。

**思路：**
1. 从右向左找第一个「上升位」i（`nums[i] < nums[i+1]`）
2. 从右向左找第一个大于 `nums[i]` 的数 j，交换
3. 将 i+1 到末尾的部分翻转（变为升序）

**展开理解：** 要得到刚好更大的排列，应尽量只改变靠右位置。从右向左找到最右侧的上升位置 i（`nums[i] < nums[i+1]`）后，右侧必为非递增；用右侧最小但大于 nums[i] 的数交换，再把后缀变为最小的升序，即得到下一个排列。

```python
def nextPermutation(nums):
    n = len(nums)
    i = n - 2
    # 找上升位
    while i >= 0 and nums[i] >= nums[i + 1]:
        i -= 1
    if i >= 0:
        j = n - 1
        while nums[j] <= nums[i]:
            j -= 1
        nums[i], nums[j] = nums[j], nums[i]
    # 翻转 i+1 到末尾
    left, right = i + 1, n - 1
    while left < right:
        nums[left], nums[right] = nums[right], nums[left]
        left += 1
        right -= 1
```

**复杂度：**
- 时间：O(n)
- 空间：O(1)

---

## 287. 寻找重复数

**题目描述：** 给定包含 n+1 个整数的数组，整数范围 [1, n]，只有一个重复的数，找出这个数（不修改数组，空间 O(1)）。

**示例：** 输入：`nums = [1,3,4,2,2]`；输出：`2`。

**思路（Floyd 判圈算法）：** 将数组视为链表（索引 → 值 → 索引），重复数对应链表的环入口。用快慢指针找环，再用双指针找入环点。

**展开理解：** 把下标看作节点、nums[i] 看作 next，取值范围保证从 0 出发会进入环；重复值意味着两个下标指向同一节点，即环入口。Floyd 两阶段分别找相遇点和入口，全程不修改数组且 O(1) 空间。

```python
def findDuplicate(nums):
    slow = fast = nums[0]
    # 找相遇点
    while True:
        slow = nums[slow]
        fast = nums[nums[fast]]
        if slow == fast:
            break
    # 找入环点
    ptr = nums[0]
    while ptr != slow:
        ptr = nums[ptr]
        slow = nums[slow]
    return slow
```

**复杂度：**
- 时间：O(n)
- 空间：O(1)
